# LitHub 帮助手册

> **LitHub** 是一条跑在单机上的文献流水线：把 **Zotero 文献库**、**MinerU 文档转换** 和
> 一个 **LLM CLI** 串起来，自动追踪某个研究方向的文献、读全文、写中文总结、按你的分类体系
> 整理进 Zotero，每天出一期带排序的文献日报。面向「一个人 + 一个 Zotero 库 + 一条 cron」，
> 不是多人服务；所有脚本只用 Python 标准库（没有 `requirements.txt`）。

**新来的先读 §1。** 那是一条照着做就能跑通的路径，30 分钟内能看到第一个成果
（一篇 PDF 变成 Zotero 里的中文总结）。跑通之后再读 §4（换成你自己的方向）和 §5（日常运转）。

## 三份文档怎么分工

仓里只有三份中文文档，**各写各的，不互相抄**：

|                | `README.md`            | `帮助手册.md`（本文）                     | `AGENTS.md`                                  |
| -------------- | ---------------------- | ----------------------------------------- | -------------------------------------------- |
| **读者**       | 路过的访客、第一次打开仓库的人 | **人**——新同学、换方向的人、回来查参数的人 | **AI 代理**——在 `~/LitHub` 里开会话时自动加载的项目约定  |
| **内容**       | 一页落地页：这仓库做什么、从哪里开始   | 上手路径、环境、换方向清单、全部参数与踩坑                    | 目录约定、脚本入口、领域配置说明、实现决策与实测记录                |
| **长度**       | 约 55 行                | 2200+ 行                                   | 约 970 行                                    |
| **详细内容放哪** | 全部指向本文（见「从这里开始」表） | 就是本文                                      | 本文不重复的实现细节与「为什么这么写」                     |

> ⚠️ **打分公式两边都有**（本文 §6.3 / `AGENTS.md` 的「每日文献日报」一节）。
> **改公式或改权重，两处必须同步**，否则两个读者会按不同的公式理解同一份日报。
> 这是全套文档里**唯一**一处刻意保留的重复。

本文的章节编号是稳定的，仓内其它文件引用本文时写成 `帮助手册.md §6.3` 这种形式。

> ⚠️ **打分公式两边都有**（本文 §6.3 / `AGENTS.md` 的「每日文献日报」一节）。
> **改公式或改权重，两处必须同步**，否则两个读者会按不同的公式理解同一份日报。

本文的章节编号是稳定的，仓内其它文件引用本文时写成 `帮助手册.md §6.3` 这种形式。

---

## 1. 第一次上手：操作指南

这一节假设：你刚拿到这个包，**研究方向与原作者不同**，想在自己电脑上跑起来。
包里没有作者的文献库、没有任何运行状态，你是真正的从零开始。

### 1.1 最短路径（约 30 分钟，看到第一个成果）

目标：**一篇 PDF → Zotero 里一条条目 + 两个链接附件**（`paper.md` 与中文 `summary.md`）。
这条链路不碰分类、不碰日报、不碰定时任务，是「最小可行链路」。
它涉及的服务全部免费或按量计费（写一篇中文总结约 **0.037–0.053 元**）。

#### 步骤 0：确认前置（5 分钟）

```bash
python3 --version                    # 需要 3.12+
which mineru-open-api                # 装了且在 PATH 上（脚本里是裸命令名）
ls ~/.kimi-code/bin/kimi             # LLM CLI 存在（可用 LITHUB_KIMI 换路径）

# Zotero 必须正在运行
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:23119/connector/ping
#   200 → Zotero 活着

# 本地 API 开关
curl -s -o /dev/null -w "%{http_code}\n" "http://localhost:23119/api/users/0/items?limit=1"
#   200 → 开关已开，可以继续
#   403 → 开关没开：Zotero → 设置 → 高级 → 勾「允许其他应用与本机 Zotero 通信」
```

**成功判据**：四条都符合预期。`403` 是最常见的卡点，**不勾这个开关后面全部脚本都会失败**。
细节见 §3.2。

#### 步骤 1：一次性写入授权（3 分钟）

```bash
cd ~/LitHub && mkdir -p state

# 先取 Server-ID（响应头里）
SID=$(curl -sI http://localhost:23119/api/users/0/collections?limit=1 \
      -H "Zotero-API-Version: 3" | grep -i '^Zotero-Server-ID:' | tr -d '\r' | awk '{print $2}')

# 再授权：Zotero 会弹一个对话框，选「始终允许」
curl -s -X POST http://localhost:23119/api/local/authorize \
  -H "Content-Type: application/json" -H "Zotero-Server-ID: $SID" \
  -d '{"appName":"LitHub"}' | python3 -c 'import sys,json;print(json.load(sys.stdin)["key"])' \
  > state/zotero_local_key
chmod 600 state/zotero_local_key
```

**成功判据**：

```bash
test -s state/zotero_local_key && echo OK && wc -c < state/zotero_local_key
```

非空即可（返回的 key 形如 `04cB9QTKO9UNhf1umsHgcYZBEsrunZ9J`）。

> ⚠️ **`state/zotero_local_key` 必须在跑任何脚本之前就存在**：`scripts/zapi.py:31-32`
> 在 **import 时**就打开这个文件并取 Server-ID，连只读的脚本也不例外。
> 缺文件或 Zotero 没开 → 直接 `FileNotFoundError` / `URLError`。
> 密钥可在 Zotero 设置里撤销；`remember: true` 表示已保存，cron 无人值守也能写。
> API 的坑（尤其是「写入返回 200 但什么都没改」）见 §7。

#### 步骤 2：建运行目录（1 分钟）

```bash
cd ~/LitHub
mkdir -p state/work logs papers 待整理 文献日报

echo '[]' > state/manifest.json          # 收编清单，intake_pdfs / mineru_batch 直接 json.load，不会自动建
echo '[]' > state/classification.json    # 分类表；缺它会让 daily_digest.py 直接崩（§5.4）
```

#### 步骤 3：扔 1 篇 PDF 进去（1 分钟）

```bash
cp /你的某篇论文.pdf 待整理/
ls 待整理/
```

#### 步骤 4：收编（2 分钟）

```bash
python3 scripts/intake_pdfs.py --dry-run    # 先看它打算做什么，不写任何东西
python3 scripts/intake_pdfs.py --remove     # 真收编；成功后删掉待整理/里的原件
```

脚本按 PDF 首页的 DOI 匹配库里已有条目，缺则用 CrossRef 元数据**新建条目**，三段式上传
PDF，并登记进 `state/manifest.json`。

**成功判据**：

```bash
python3 -c "import json;m=json.load(open('state/manifest.json'));print(len(m),[r['status'] for r in m])"
# → 1 ['pdf']
```

> 作者接受稿（OSTI／仓库版）常整篇不含 DOI，首页提取不到就会被跳过。这类在
> `待整理/_doi_hints.json` 里补一行 `"<文件名>": "<DOI>"`，脚本优先用它（提示优先于文本提取）。

#### 步骤 5：转 Markdown（实测 28 s–324 s/篇）

```bash
python3 scripts/mineru_batch.py all 3
```

**成功判据**：末尾打印 `完成：成功 1，跳过 0，失败 0`，且

```bash
ls papers/                      # → <KEY>_<标题slug>/
ls papers/<KEY>_*/              # → paper.md（+ images/，精准模式才有图）
sed -n 1,8p papers/<KEY>_*/paper.md   # → YAML front-matter，第 7 行是 engine: extract
```

单篇实测耗时 **28 s–324 s**（`extract` 精准模式）。引擎差异与 token 的坑见 §8。

#### 步骤 6：写中文总结（约 35 秒/篇，约 0.04 元）

```bash
python3 scripts/summarize_batch.py 1        # 1 = 并发数；只跑这一篇
# 想只跑某篇：python3 scripts/summarize_batch.py 1 <KEY>
```

**成功判据**：末尾打印 `完成：成功 1，跳过 0，无源 0，失败 0`，且

```bash
grep -c '^## ' papers/<KEY>_*/summary.md    # → 7（固定七节）
head -12 papers/<KEY>_*/summary.md          # → YAML 属性区由脚本从 Zotero 写入，不是模型写的
```

若打印的是 `失败 1` 且状态是 `EXC(...)`，**先查 LLM CLI 路径**（见 §3.4）。
若状态是「未落笔」，说明模型把步数花在读上还没写，见 §6.5。

#### 步骤 7：挂回 Zotero（10 秒）

```bash
python3 scripts/link_markdown.py
```

**成功判据**：末尾打印 `完成：创建 2/2 个链接附件`。到 Zotero 里点开那条文献，
下面应出现两个 `linked_file` 子附件：「全文 Markdown」→ `paper.md`、「中文总结」→ `summary.md`。

**至此最短路径跑通。** 同一批 PDF 想全跑一遍，就把步骤 4–7 的并发数调大
（`mineru_batch.py all 3`、`summarize_batch.py 3`）。

### 1.2 完整路径（到第一期日报）

⚠️ **顺序有依赖，别跳步。** 前 8 步是「把库填起来」，9–13 步是「给日报备料」，14 步才是日报。

| # | 命令 | 需要什么 | 产出什么 |
|---|---|---|---|
| 0 | Zotero 运行 + 授权（§1.1 步骤 0–1） | — | `state/zotero_local_key` |
| 1 | 改两个配置：`config/topic.json`（§4.1）+ `config/runtime.json`（§4.3） | 你的领域知识 | — |
| 2 | `python3 scripts/intake_pdfs.py --dry-run` → `--remove` | `待整理/*.pdf`、`state/manifest.json` | Zotero 条目 + PDF 附件，写入 manifest |
| 3 | **手工写 `state/classification.json`** | Zotero 里的 key/标题 | —（格式见 §5.4） |
| 4 | `python3 scripts/apply_to_zotero.py` | `classification.json`、`topic.json` | Zotero 分类目录 + 标签轴 |
| 5 | `python3 scripts/sync_classification.py` | `classification.json` | `分类与标签总表.md`（只读分类数据，换方向不用改） |
| 6 | `python3 scripts/mineru_batch.py all 3` | `manifest.json` 里 `status=="pdf"` 的条目 | `papers/<KEY>_.../paper.md` + `images/` |
| 7 | `python3 scripts/summarize_batch.py 3` | `manifest.json`、Zotero、`prompts/summarize.md`、LLM CLI | `summary.md` |
| 8 | `python3 scripts/link_markdown.py` | `manifest.json`、Zotero | linked_file 附件挂回条目 |
| 9 | `python3 scripts/digest.py json` | `papers/*/summary.md` | `state/digest.json` |
| 10 | `python3 scripts/build_profile.py` | `classification.json` **和** `digest.json` | `state/profile.json`、`文献画像.md` |
| 11 | `python3 scripts/library_index.py` | Zotero + `topic.json` | `state/library_index.json` |
| 12 | `python3 scripts/citation_graph.py` | `library_index.json` | `state/citation_graph.json` |
| 13 | `python3 scripts/journal_if.py` | `topic.json` | `state/journal_if.json`（期刊层级，年报刷新） |
| 14 | `python3 scripts/daily_digest.py`（裸跑即用 `runtime.json` 的工作点） | 上面全部 | `文献日报/YYYY-MM-DD.md` + `.urls.txt` |
| 15 | `python3 scripts/tidy.py --apply` | Zotero | 清理 + 备份 |

> 第 14 步**第一次跑之前务必先改 `runtime.json` 的 `cat_floor`**（改成你的分类或留空 `""`）。
> 留着作者的分类名会直接报错退出——那些分类不在你的 `category_order` 里。
> 这是刻意设计的：宁可不跑，也不静默忽略一个你以为是生效的配置。

日报出不来或候选极少，按 §10 的顺序排（**先保证候选够，再动权重**）。
想让日报自己每天跑，配 §9 的 systemd timer。

### 1.3 三个必改的地方

换方向的完整清单在 §4.2（按符号名定位，共 15 处），但**这三个不改，日报就一直按作者的方向筛**：

| # | 位置 | 为什么必改 |
|---|---|---|
| 1 | `config/topic.json` **整份重写** | 「这个库研究什么」的唯一权威来源：分类法、检索式、判分关键词、闸门正则、期刊白名单。**改完必须重跑 `build_profile.py`**，检索式经 `state/profile.json` 才进日报 |
| 2 | `config/runtime.json` 的 `digest.cat_floor` | 代码里的默认值是**空串**（分类名属领域内容，代码不含任何分类名）。留着作者的分类名会**启动即报错退出**。要么写成你的分类，要么留 `""` 关闭保底 |
| 3 | 代码里剩余的领域硬编码：`daily_digest.py` 的 `LAYERED_PAT`、`find_new.py` 的 `relevant()`、翻译系统提示词 `sys_p` | `topic.json` 之外**没有抽干净**。最硬的一道是 `LAYERED_PAT`（强制体系闸门：标题或摘要不命中就直接丢弃）。清单与改法见 §4.2 |

### 1.4 自检清单

跑完之后用这几条确认没走偏：

```bash
cd ~/LitHub

# 1) papers/ 目录数 = 库内顶层条目数（只建了条目的，link_markdown.py 会报「缺文件」）
ls -d papers/*/ | wc -l

# 2) 每篇都是「已入库」状态：paper.md 与 summary.md 都在
python3 - <<'EOF'
import glob, os
bad = [d for d in glob.glob('papers/*/')
       if not (os.path.exists(d+'paper.md') and os.path.exists(d+'summary.md'))]
print('缺文件：', bad or '无')
EOF

# 3) classification.json 覆盖了每一篇
python3 -c "import json,glob;m=json.load(open('state/classification.json'));print(len(glob.glob('papers/*/')),'篇 / 分类',len(m),'条')"

# 4) 日报确实产出了
ls -l 文献日报/ | tail -3

# 5) 生效的运行参数（不是你以为的那份）
python3 scripts/daily_digest.py --show-config

# 6) Zotero 侧：没有空条目与重复条目（默认只报告）
python3 scripts/tidy.py
```

> **「入库」的判据**（作者口径）：**跑到 `mineru_batch.py`（转 Markdown）和
> `summarize_batch.py`（写中文总结）才算入库**，只建条目 + 挂 PDF 不算。
> 自查：`papers/` 目录数 = 库内顶层条目数，且每篇 `paper.md` 与 `summary.md` 都在；
> 只建了条目的，`link_markdown.py` 会报「缺文件」。
> （2026-09-16 起因「新建条目就算是入库」被纠正，勿再按该口径交付。）

### 1.5 建议的推进节奏

**先跑通窄链路，再加自动化。** 不要一上来就配 systemd、调权重。

1. 读完本文 §1–§3（尤其 §3.2 的授权与 §3.4 的两套 LLM 机制）。
2. 打通**最窄的一条链路**（就是 §1.1）：Zotero 授权 → 手工丢 1–2 篇 PDF 进 `待整理/` →
   `intake_pdfs.py` → 手写 `classification.json` → `apply_to_zotero.py` → `mineru_batch.py` →
   `summarize_batch.py` → `link_markdown.py`。
3. 看 `examples/` 的输出（一篇 CC BY 4.0 论文经流水线处理后的完整产出），对照
   `python3 scripts/summarize_batch.py --dry-run 1 <KEY>` 打印的最终提示词，确认模板符合你的方向
   （改 `prompts/summarize.md` 和 `summarize_batch.py` 的 `SECTIONS`）。
4. 等库里有 **30–50 篇**之后，再重写 `config/topic.json`、跑 `build_profile.py`、
   `library_index.py`、`citation_graph.py`、`journal_if.py`。画像需要足够的语料才有意义。
5. 最后才碰日报：先把 `config/runtime.json` 的 `cat_floor` 改成你的分类（或留空），
   再 `--dry-run --diag-cats` 调检索式，然后标定 `min_score`，最后配 systemd
   （unit 里**只写裸命令**，工作点都在 `runtime.json`）。
6. 每一步都用 `--dry-run` 先看效果；`tidy.py --apply` 是唯一会删东西的命令，跑前确认。

---

## 2. 这个项目是什么

一条跑在**单机**上的文献流水线，把三样东西串起来：

```
Zotero 文献库  ──►  主动检索/日报  ──►  手工下载 PDF
       ▲                                    │
       │                                    ▼
   分类目录+标签 ◄── 分类  ◄── 中文总结 ◄── MinerU 转 Markdown
       ▲
       └── 引文图 / 画像 / 语义向量（日报的排序依据）
```

- **设计目标**：一个人 + 一个 Zotero 库 + 一条 cron 定时任务；所有脚本**幂等、可续跑**
  （做完的跳过），状态全部落在 `state/`。
- **技术栈**：Python 3.12+，**只用标准库**（没有 `requirements.txt`）；外部依赖是三个 CLI：
  Zotero 桌面版、MinerU、一个 LLM CLI。
- **领域无关的部分**：「这个库研究什么」集中在 `config/topic.json`；「日报怎么跑」集中在
  `config/runtime.json`。包里带的是作者的方向（锂离子电池层状氧化物正极），
  **你要把 `topic.json` 整份重写成你领域的**（§4）。
- **不含的部分**：论文 PDF / 全文、Zotero 库本身、任何运行状态、密钥。见 §11。

### 2.1 目录速查

| 路径 | 内容 | 包里有没有 |
|---|---|---|
| `scripts/` | 全部脚本 | ✅ |
| `config/topic.json` | **领域配置**（分类法/子类与五个标签轴词表/检索式/关键词/闸门/期刊表）——改完要重跑画像 | ✅ 当模板 |
| `config/runtime.json` | **运行配置**（频率/篇数/检索池/保底/打分权重/发信）——改完立即生效 | ✅ 当模板 |
| `prompts/summarize.md` | 中文总结的提示词模板 | ✅ |
| `examples/` | 一篇 CC BY 论文经流水线处理后的完整产出（`paper.md`+`summary.md`+图） | ✅ |
| `papers/<KEY>_<slug>/` | 每篇的 `paper.md` / `summary.md` / `images/` | ❌ 要自己生成 |
| `state/` | 全部运行状态与缓存 | ❌ 要自己生成 |
| `文献日报/`、`logs/` | 日报与日志 | ❌ 要自己生成 |
| `待整理/` | 收件箱：手工下载的 PDF 丢这里 | ❌ 自己建 |
| `帮助手册.md` | **本文**：人的上手手册（环境、换方向、全部参数、踩坑） | ✅ 必读 |
| `AGENTS.md` | AI 代理的项目约定（目录结构、脚本入口、领域配置、实现决策） | ✅ 必读 |
| `README.md` | 一页落地页：做什么 + 从哪里开始（详细内容一律指向本文，不重复） | ✅ |

---

## 3. 环境与前置

### 3.1 Python

```bash
python3 --version    # 需要 3.12+
```

**无需 pip install 任何东西**就能跑主管线。唯一可选的第三方依赖是日报的语义相似度项（`S`）：

```bash
# 可选，不装也能跑（S 项自动跳过，权重回补给 R）
pip install torch --index-url https://download.pytorch.org/whl/cpu
pip install sentence-transformers
export HF_ENDPOINT=https://hf-mirror.com   # 国内网络必需，否则下不到模型权重
```

装完跑 `python3 scripts/build_embeddings.py` 生成 `state/work/lib_emb.npz`。
⚠️ **启用 `S` 前必须重标定 `--min-score`**，见 §6.3。

### 3.2 Zotero 桌面版 + 本地 API 授权

这是最容易卡住的一步，**做不完全部脚本都会失败**。

1. 装并**保持运行 Zotero 桌面版**（脚本连的是 `http://localhost:23119/api/users/0`，
   写死在 `scripts/zapi.py:20` 与 `scripts/apply_to_zotero.py:10`）。
2. Zotero → *设置 → 高级* → 勾选 **「允许其他应用与本机 Zotero 通信」**。不勾会返回 403。
3. 取 `Zotero-Server-ID` 并做一次性写入授权，把返回的 key 存进 `state/zotero_local_key`
   （命令见 §1.1 步骤 1；`chmod 600`）。

> ⚠️ **`state/zotero_local_key` 必须在跑任何脚本之前就存在**：`scripts/zapi.py:31-32`
> 在 **import 时**就打开这个文件并取 Server-ID，连只读的脚本也不例外。
> 缺文件或 Zotero 没开 → 直接 `FileNotFoundError` / `URLError`。

踩坑与错误码速查见 **§7**（尤其是「写入返回 200 但没生效」那类静默失败）。

### 3.3 MinerU CLI

```bash
# 装好 mineru-open-api 并确保在 PATH 上（脚本里是裸命令名）
which mineru-open-api

# 配 token（默认的 extract 精准模式需要）
mineru-open-api auth
```

- **默认走 `extract`（精准模式）**：需要 token，限额 200 MB / 600 页，导出 `images/`、
  识别公式表格。token 在 <https://mineru.net/apiManage/token> 申请，
  **正确长度 51 字符**（复制不全是最常见的失败原因）。
- `--flash` 是免 token 兜底：限额 10 MB / 20 页，**没有图片**，只有占位符。
- 详细踩坑见 **§8**。

### 3.4 LLM CLI

项目里用了**两套不同的 LLM 机制**，别混：

| 用途 | 机制 | 配置 |
|---|---|---|
| 写中文总结、挑配图 | 调外部 CLI（默认 `~/.kimi-code/bin/kimi`，可用环境变量 `LITHUB_KIMI` 覆盖） | `scripts/summarize_batch.py:24`、`scripts/embed_figures.py:26` |
| 日报里翻译标题/写提要 | **直接 HTTP** 调 DeepSeek，读 `~/.kimi-code/config.toml` 里的 `[providers.deepseek].api_key` | `scripts/daily_digest.py:602-699` |

- 总结的调用形式是 `kimi -p "<提示词>"`，**工作目录 = 该篇的 `papers/<KEY>_.../`**，
  所以提示词里说「读当前目录下的 paper.md」。
- CLI 不存在时不会整批崩，而是每篇报 `EXC(...)`——**看到一堆 EXC 先查 CLI 路径**。
- 没有 DeepSeek key 也能跑日报，只是标题退回英文、没有中文提要。

### 3.5 环境变量

| 变量 | 用途 | 默认 |
|---|---|---|
| `LITHUB_MAILTO` | 调 Crossref / Unpaywall / OpenAlex / Semantic Scholar 时带的邮箱（礼貌池要求） | `you@example.com` → **务必设成你自己的** |
| `LITHUB_UA` | 覆盖 HTTP User-Agent（部分出版方按 UA 拦截） | `LitHub/1.0 (mailto:...)` |
| `LITHUB_KIMI` | 总结用的 LLM CLI 路径 | `~/.kimi-code/bin/kimi` |
| `LITHUB_TOPIC` | 换一份领域配置（多主题并存时用） | `~/LitHub/config/topic.json` |
| `LITHUB_RUNTIME` | 换一份运行配置（多套工作点并存时用） | `~/LitHub/config/runtime.json` |
| `HF_ENDPOINT` | 国内下 HuggingFace 模型权重 | 无（需显式设 `https://hf-mirror.com`） |

建议写进 `~/.bashrc`：

```bash
export LITHUB_MAILTO="你的邮箱@example.com"
export HF_ENDPOINT=https://hf-mirror.com
```

### 3.6 系统工具

```bash
sudo apt install ghostscript poppler-utils   # gs（PDF 预压缩）、pdfinfo/pdftotext（页数与 DOI 提取）
```

### 3.7 ⚠️ 项目的根目录必须是 `~/LitHub`

很多脚本用 `os.path.expanduser("~/LitHub")` 解析根目录（如 `scripts/zapi.py:21`、
`scripts/digest.py:5`、`scripts/topic.py:20`），另一些用 `__file__` 推导——**两套机制并存**。
放到别处会有一半脚本找不到文件。**老老实实放 `~/LitHub`。**

### 3.8 ⚠️ IPv6 陷阱

本机踩过的坑：**有全局 IPv6 地址和默认路由，但实际 100% 丢包**。Python 没有 curl 那样的
happy-eyeballs 回退，会一直卡到超时（实测同一请求 IPv6 **181 秒** vs 强制 IPv4 **0.72 秒**）。
项目已在 `scripts/zapi.py` 里强制 IPv4，但**根治法**是：

```bash
echo 'precedence ::ffff:0:0/96  100' | sudo tee -a /etc/gai.conf
```

写自己的网络代码时显式强制 IPv4：

```python
import socket
_orig = socket.getaddrinfo
def _v4(host, port, family=0, *a, **k):
    return [r for r in _orig(host, port, family, *a, **k) if r[0] == socket.AF_INET]
socket.getaddrinfo = _v4
```

### 3.9 ⚠️ Unpaywall 会硬拒占位邮箱（重要）

`LITHUB_MAILTO` 不设时默认是 `you@example.com`，**Unpaywall 直接 422 拒绝**：

```
HTTP 422  {"message": "Please use your own email address in API calls."}
```

后果比报错更隐蔽：`cached_json` 会先按退避策略**重试 3 次（3s + 6s）才放弃**，
所以 `oa_info()` 每篇要多等 **~12 秒**，而 `A`（是否开放获取）**永远拿到 0**——
实测换成真邮箱后同一请求 1 秒返回、且 `is_oa` 正确。

- 手动跑：`export LITHUB_MAILTO=你的邮箱`
- systemd **不读 `.bashrc`**，两个 unit 的 `Environment=` 里都写死了这个变量；
  漏写就会静默退化（不报错，只是慢且 A 恒为 0）
- Crossref / OpenAlex 对占位邮箱不硬拒，所以只有走 Unpaywall 的功能会中招

### 3.10 国内网络

- pip 已配阿里云镜像（`~/.config/pip/pip.conf`）
- HuggingFace 不可达，需 `export HF_ENDPOINT=https://hf-mirror.com`
- MinerU 的 `flash-extract` 有 **10 MB 且 20 页**双限制，且**文件大小先于页数触发**，
  `--pages` 绕不过 10 MB。超过 8 MB 的 PDF 先用 ghostscript 预压缩：
  `gs -dPDFSETTINGS=/ebook`
  → 这些限制**只针对 `--flash` 兜底模式**；默认的 `extract` 是 200 MB / 600 页，
  不用压缩也不用分块（见 §8.2）。
- MinerU 限流（官方）：提交任务 50 文件/分钟、每日 5000 文件（html ≤100）；
  取结果 1000 次/分钟。目前无收费计划。
- OpenAlex 2026 起按请求计费（$0.001/条，匿名池每天 $0.1 ≈ 100 条），额度耗尽返回
  429 + `retry-after`，重置在**次日 00:00 UTC**（= 08:00 CST）。见 §6.7。

---

## 4. 换成你自己的研究方向

**这是本包最重要的部分。** 包的代码是领域无关的骨架，但「研究什么」和一部分判定词
是照着作者的锂电正极方向写的。

### 4.1 重写 `config/topic.json`

这是「研究什么」的唯一权威来源，装载器是 `scripts/topic.py`（只读 + 编译正则，
不含领域内容）。路径可用环境变量 `LITHUB_TOPIC` 覆盖，便于多主题并存。

| 字段 | 作用 | 你要做什么 | 谁在用 |
|---|---|---|---|
| `name` | 主题名，日志里显示 | 改成你的方向 | 日志 |
| `category_order` | 一级分类及**版面顺序**，也是 Zotero 一级目录名 | 定义你的 4–8 个分类 | `daily_digest.py` 的 `CAT_ORDER` |
| `categories.<类>.sub` | 子类标签（建 Zotero 子目录 + 打标签用） | 每类 3–8 个 | `apply_to_zotero.py` 的 `TAXONOMY` |
| `categories.<类>.queries` | **该类检索式**——日报候选的主要来源 | 每类 3–5 条，见 §4.1.1 | `build_profile.py` → `profile.json` → `daily_digest.py` |
| `categories.<类>.keywords` | 该类**判分关键词**（小写子串匹配） | 覆盖该类核心术语 | `daily_digest.py` 的 `CAT_KEYS` |
| `categories.<类>.pref` | 手调偏好系数（1.0 = 中性），乘进分类权重 | 想多推的方向调到 1.3–1.7 | `build_profile.py` 的 `CAT_PREF` |
| `labels.sub` / `.method` / `.materials` / `.form` / `.system` | 五个标签轴的**关键词表，键即标签名**（空值 = 该标签只允许手标） | 子类键要与 `categories.<类>.sub` 对齐 | `daily_digest.py` 给库外候选打标签；`apply_to_zotero.py` 校验取值 |
| `coupling` | `耦合:<方向>` 附加标签轴取值（**只标注、不参与日报打分**） | 用不上可留空数组 `[]` | `apply_to_zotero.py` |
| `journals` | 期刊白名单（前缀匹配） | 换成你领域的刊，**这一步没做对日报会几乎没候选** | `find_new.py` / `journal_if.py` |
| `gates.topic` | 必须命中的**体系词**（最严） | 你的体系核心词 | `find_new.py` → `daily_digest.py` |
| `gates.topic_theory` | 「基础理论」类额外要落回的体系词（可以更宽） | 通常比 `topic` 宽 | 同上 |
| `gates.must` | 体系自证词（宽） | — | 同上 |
| `gates.strong` | 体系自证词（严） | — | 同上 |
| `gates.exclude` | **标题级**排除（邻近领域） | 例如作者做锂电就把钠电/锌电/超级电容全排除 | 同上 |
| `gates.offtopic` | **摘要级**离题词 | — | 同上 |
| `extra_categories` | 综述等其他目录 | 一般保留 `["综述"]` | `apply_to_zotero.py` / `daily_digest.py` |
| `fallback_category` | 兜底分类 | 设成你最大的那类 | `apply_to_zotero.py` / `daily_digest.py` |
| `theory_category` | 计算类论文落回哪一类 | 同上 | 同「计算类 → `theory_category`」判定 |
| `archive_collections` | 归档目录名，其下整棵子树是历史项目：`tidy.py` 不碰、也不扫描 | 保留 `["归档"]` | `tidy.py` 的 `ARCHIVE_NAMES` |

**所有闸门正则都是 `re.I` 编译**（`scripts/topic.py:48-60`），写小写即可。

改完**先跑 `build_profile.py`**——检索式经 `state/profile.json` 才进日报
（`daily_digest.py` 只在 `profile.json` 缺失时才退回 `find_new.QUERIES`）。

> 关于 `extra_categories` 里的「其他」：作者已于 2026-09-20 删掉。它从没装过东西，
> 而 `fallback_category` 是「结构退化」，自动分类不会落到它上面；
> `apply_to_zotero.py` 每轮建、`tidy.py` 每轮删，纯属互相抵消。你换方向时不必照抄这个名字。

**路线图（未实现）：`scripts/bootstrap_topic.py`** —— 让别人不必手写这个 json。
输入 20–50 篇种子（DOI 列表，或一个 Zotero collection），自动产出 `topic.json` 初稿：

1. 取种子标题+摘要，LLM 归纳出 4–8 个一级类，每类 5–8 个子类标签
2. 每类生成 3–5 条检索式（复用 `CAT_KEYS` 风格的术语）
3. 从库内高频 n-gram 抽「必须词」，从邻近领域抽「离题词」，拼出 `gates`
4. 期刊白名单 = 库内已收期刊 ∪ OpenAlex 同领域刊（`journal_if.py` 已能查）
5. 冷启动没有库时，退化到种子扩充：用 Semantic Scholar 的 references/citations
   做两层引文图扩张，把结果当临时库喂给上面同一套逻辑

⚠️ 自动生成的质量在冷启动时不稳定——画像那套「热点词扩写」就是前车之鉴：
`phrases()` 不滤数字与单位，抽出来的全是 `mah g` / `3 v` 这类碎片，那条路**从未生效过**，
两节输出已在 2026-09-20 从画像里撤掉（详见 §6.7）。
定位是**自动草稿 + 人工过一遍**，不是全自动。另外换领域后
`--rows / --pages / --min-score` 这些工作点要重新标定，不能照搬。

#### 4.1.1 怎么定检索式（实测经验）

**不要凭感觉写。** 直接量产率：拉 100 条候选，过期刊白名单 + 体系正则 + 相关性闸门后
再分类，数「过闸篇数 / 其中你目标分类的篇数 / 最好排名」。

```bash
python3 scripts/daily_digest.py --dry-run --diag-cats --date 2026-01-01
```

`--diag-cats` 会打印每个分类的候选数量、命中来源（哪条检索式捞到的）、以及最高分离入选线差多少。

另一种等价做法（作者标定力学方向时用的）：`search_crossref` 拉 100 条，
过 `journal_ok` + `LAYERED_PAT` + `relevant()` 后再 `classify()`，
数「过闸篇数 / 其中目标分类篇数 / 最好排名」。

**关键教训（作者调力学方向时实测）**：只把某类权重乘到 2.5 倍，该类候选 85 篇里只有
19 篇来自该类检索式，最高分（0.326）仍低于入选线——**加权重换不来文献，必须同时加检索式**。
补了 4 条检索式后该类候选 96 篇、35 篇来自新检索式，最高分升到 0.481，
入选数从 1 升到 3。另外实测 `chemo-mechanical degradation Ni-rich cathode` 一次给
10 篇目标类文献，而 `stress corrosion cracking cathode electrolyte cathode` 给 0 篇——
**检索式的措辞直接决定产率**。

### 4.2 ⚠️ 换方向**必须同时改**的硬编码

`topic.json` 之外的领域内容**没有全部抽干净**。下面这些地方写死了锂电正极的东西，
你要逐个改，否则日报会一直按作者的方向筛。

下表按**符号名**定位（行号会随改动漂，搜符号名更可靠）。五个标签轴词表与 `coupling`
**已经不在代码里**——它们在 `topic.json` 的 `labels.*` 与 `coupling`（见 §4.1）；
`--cat-floor` 的默认值也已搬进 `runtime.json`（见 §4.3）。

| 文件 | 符号 / 位置 | 内容 | 必改程度 |
|---|---|---|---|
| `scripts/daily_digest.py` | `LAYERED_PAT` | **强制体系闸门**：标题或摘要不命中就直接丢弃。全项目最硬的一道领域闸 | **必改** |
| 〃 | `translate()` 里的 `sys_p` | 翻译系统提示词写死「你是锂电正极材料的科技翻译」 | **必改** |
| 〃 | `classify()` 里的理论方法正则 | 「计算类 → `theory_category`」的判定词（dft/simulation/model/machine learning…） | 建议改 |
| 〃 | `REVIEW_PAT` | 综述识别正则（review/perspective/roadmap 等英文通用词，多数领域可不动） | 一般不用改 |
| `scripts/find_new.py` | `relevant()` 前段 | 负极主导的条目、卤化物/有机/普鲁士蓝/硫正极等排除 | **必改** |
| 〃 | `relevant()` 的 `if cat == "基础理论"` 分支 | 写死分类名 + 理论方法正则 + 钠电排除 —— 分类名要换成你的 | **必改** |
| 〃 | `relevant()` 末段 | `MUST_TOPIC` / `MUST_LI` / `LITHIUM_STRONG` 的组合校验与钠电排除 | **必改** |
| `scripts/build_profile.py` | `STOP` | 停用词表（含 battery / cathode / lithium-ion / layered oxide） | 建议改 |
| 〃 | 热点短语白名单 | cathode / layered / oxygen / interphase / crack / diffusion / strain / redox | 建议改 |
| 〃 | 三处 `>= 2024` | 「近三年活跃」阈值写死年份 | 建议改 |
| `scripts/journal_if.py` | `J_ABBR` / `NAME_ALIAS` | 期刊名缩写表与别名（决定引文通道能不能认出参考文献的刊） | 建议改 |
| `scripts/summarize_batch.py` | `PROMPT` 兜底常量 / `SECTIONS` | 与 `prompts/summarize.md` 同步的内置兜底；七节标题已中性，改模板措辞时要同步这里 | 建议改 |
| 〃 | `library_context()` / `REVIEW_MAX` | 「库内同主题工作」清单的打分与篇数（3×子类 + 2×材料 + 1×形态 + 1×同分类，≥3），分类法差异大时要重标 | 建议改 |
| `prompts/summarize.md` | 全文 | 提示词模板（七节结构已中性，措辞/单位举例按你的领域调） | 建议改 |
| `scripts/migrate_labels.py` | 全文件 | 一次性标签迁移（`RENAMES` / `DROPS` / `COLLECTIONS`）。换主题后没有旧标签要清，**可整文件删掉** | 可弃用 |

> `scripts/sync_classification.py`（把 `classification.json` 渲染成总表）**不在上表里**
> ——它 2026-09-20 已清干净：分类法与标签轴全部从 `config/topic.json` 读，不写死领域内容，
> 换方向不用改。`scripts/fix_zotero.py` 已删除（一次性迁移脚本，对别人无用途）。

> **策略建议**：**先把 `LAYERED_PAT`、`relevant()` 的排除规则、翻译提示词这三处改掉**，
> 管线就能跑通；五个标签词表（`topic.json` 的 `labels.*`）再按你的分类补——
> 标签名、目录名、`classification.json` 的取值**三处要一起改**。
> 总结的七节标题（含「与研究主题的关联」）已做成与方向无关，**换方向不用改**；
> `library_context()` 的打分阈值在分类法差异大的领域可能要重标。

### 4.3 运行参数：`config/runtime.json`

「日报怎么跑」全在这一个文件里，装载器是 `scripts/runtime.py`（路径可用 `LITHUB_RUNTIME`
覆盖）。它与 `topic.json` 的分工只看一件事——**改完要不要重跑画像**：

| | `topic.json` | `runtime.json` |
|---|---|---|
| 管什么 | 研究什么（分类法 / 检索式 / 关键词 / 闸门 / 期刊白名单） | 怎么跑（频率 / 篇数 / 检索池 / 保底 / 打分权重 / 发信） |
| 改完 | **必须重跑 `build_profile.py`** | **立即生效**，不碰画像 |

四段（`schedule` / `digest` / `pool` / `scoring`）+ 发信段（`email`），
**每个字段的作用、当前默认值与调参经验见 §6.2**。

- 优先级 **CLI 参数 > `runtime.json` > 内置默认**。内置默认与原硬编码**逐字一致**，
  所以这个文件缺失也不会崩（`runtime.py` 会把 `LOADED=False` 报出来）。
- **每个字段的说明就在文件里**：每段的 `_help` 是「字段名 → 说明」的字典，装载器只当注释读掉
  （不校验、不进配置、不出现在 `--show-config` 里）。JSON 没有注释语法，所以沿用了
  `topic.json` 的 `_comment` 那套下划线约定；新增字段时顺手补 `_help`。
- 查生效值：`python3 scripts/daily_digest.py --show-config`（或 `python3 scripts/runtime.py`）。
- **校验从严**：未知键、类型不对（`true` 不会被当 1 收下）、越界值**启动即报错**
  （`ValueError`），绝不静默忽略；只有两条权重不变量
  （`w_r+w_c+w_x+w_j+w_a ≈ 1.0`、`w_s ≤ w_r`）是**警告**而非报错，
  因为改了它们会让 `--min-score` 的刻度跟着变，属于「有救但必须让人看见」。
- ⚠️ `cat_floor` 在**代码里的默认值是空串**（代码不含任何分类名，分类名属领域内容）。
  换领域后**第一件事就是改它**——要么写成你的分类，要么留 `""` 关闭保底；
  留着作者的分类名会直接报错退出（这其实是好事，好过静默不生效）。
- 频率闸门：`schedule.enabled` / `every_n_days` 由 `daily_digest.py` 自己判断，
  **systemd timer 照旧每天触发**，不需要去 enable/disable timer（见 §9.2）。
  上次成功出报的日期记在 `state/last_push.json`，`--dry-run` / 显式 `--date` / `--force`
  三者一律放行。
- ⚠️ **systemd unit 的 `ExecStart` 里若写死了 `--days`/`--limit`，会盖过本文件**（CLI 优先）。
  要让配置说了算，先把这些参数从 unit 里删掉。

### 4.4 打分工作点要重标定

日报的公式见 **§6.3**。`runtime.json` 里这些值（也能用同名 CLI 参数临时覆盖）
**是照着作者的方向标定的，不能照搬**：

| `runtime.json` 的键 | 同名 CLI | 内置默认 | 说明 |
|---|---|---|---|
| `digest.min_score` | `--min-score` | `0.04` | 硬门槛。**换领域后必须重新标定**；`0` 可临时关闭 |
| `digest.cat_floor` | `--cat-floor` | `""`（代码默认空串） | **分类名必须来自你的 `category_order`**，否则报错退出 |
| `digest.days` / `digest.limit` | `--days` / `--limit` | `14` / `16` | 新鲜窗口 / 每期篇数 |
| `digest.cooldown_days` | `--cooldown-days` | `60` | 同一 DOI 多久内不重推 |
| `digest.oa_top` | `--oa-top` | `48` | 查开放获取的名额 |
| `pool.rows` / `pool.pages` | `--rows` / `--pages` | `100` / `1` | fresh 通道取数 |
| `pool.query_pages` | `--query-pages` | `3` | query 通道取数 |
| `pool.refresh_days` | — | `30` | query/refs 候选池的缓存刷新周期（天） |
| `scoring.c_age_tau_y` / `scoring.c_age_floor` | `--citer-years` / `--citer-floor` | `3.0` / `0.75` | 库内被引者的年龄衰减与地板 |

`scoring` 段另有 6 个权重（`w_r` / `w_s` / `w_c` / `w_x` / `w_j` / `w_a`）和分量常数
（`f_gain` / `f_half_life` / `x_sat` / `j_if_zero` / `j_if_top` / `c_n_sat` / `c_w_floor` /
`c_age_floor` / `c_age_tau_y`），原先是 `daily_digest.py` 的模块级常量。
**它们是一起标定的，动一个就会让 `min_score` 失配**（§6.2 有每个常数的含义）。

> ⚠️ **如果启用 `S` 项（装了 sentence-transformers），必须重新标定 `--min-score`**：
> 有 `S` 时几乎所有候选都白拿 `0.15×S`，实测（stub 向量）门槛挡下的数量从 **177/1426**
> 掉到 **13/1426**。`0.04` 是**无 `S` 时**标定的。

**先跑通再加权重。** 排查顺序：
`--dry-run --diag-cats` 看候选够不够 → 不够就补检索式/加期刊 → 够但排序不对才动权重。

---

## 5. 从零启动与日常运转

### 5.1 安装与目录准备

```bash
# 1) 解压到 ~/LitHub
git clone <你的仓库> ~/LitHub 2>/dev/null || { mkdir -p ~/LitHub && cp -r <解压目录>/. ~/LitHub/; }
cd ~/LitHub

# 2) 建运行目录（包里没有，全部要自己生成）
mkdir -p state/work logs papers 待整理 文献日报

# 3) 初始化两个必须手工建的文件
echo '[]' > state/manifest.json          # 收编清单，intake_pdfs / mineru_batch 直接 json.load，不会自动建
echo '[]' > state/classification.json    # 分类表，见 §5.4 与 §4

# 4) 确认 Zotero 在跑 + 已授权（见 §3.2）

# 5) 两个配置文件随包带过来了，但内容是我的方向，必须先按 §4 改：
#    config/topic.json    领域内容（改完要重跑 build_profile.py）
#    config/runtime.json  运行参数（改完立即生效，不必重跑画像）
python3 scripts/daily_digest.py --show-config    # 先看一遍生效的运行参数
```

### 5.2 启动顺序与依赖

**完整命令表在 §1.2**（16 步，含每步的输入与产出）。这里给表里放不下的细节。

**依赖关系**（不满足就会失败，且多数失败不是报错而是静默降级）：

- 第 2 步（`intake_pdfs.py`）要求 `state/zotero_local_key` 已存在、Zotero 在运行（§3.2）。
- 第 3 步的 `classification.json` **是唯一必须人工撰写的核心数据**，见 §5.4。
- 第 6–8 步只处理 `manifest.json` 里 `status == "pdf"` 的条目。
- 第 10 步（画像）需要 `classification.json` **和** `digest.json`（第 9 步产出）**都在**，
  否则画像的对应小节会缺。
- 第 12 步（引文图）需要第 11 步的 `library_index.json`。
- 第 14 步（日报）需要上面全部；**第一次跑之前务必先改 `runtime.json` 的 `cat_floor`**，
  留着作者的分类名会直接报错退出。
- 第 15 步（`tidy.py --apply`）是唯一会删东西的命令，跑前先看默认的报告。

**必须记牢的判据与顺序细节**：

> **「入库」的判据**：**跑到 `mineru_batch.py`（转 Markdown）和 `summarize_batch.py`
> （写中文总结）才算入库**，只建条目 + 挂 PDF 不算。自查：`papers/` 目录数 = 库内顶层条目数，
> 且每篇 `paper.md` 与 `summary.md` 都在；只建了条目的，`link_markdown.py` 会报「缺文件」。

**收编手工下载的 PDF**：出版方的作者接受稿常整篇不含 DOI，首页提取不到就会被跳过。
这类在 `待整理/_doi_hints.json` 里补一行：

```json
{"文件名.pdf": "10.xxxx/xxxxx"}
```

脚本优先用它（提示优先于文本提取）。

### 5.3 日常运转

```bash
cd ~/LitHub

# 每天/每次收编：PDF 丢进 待整理/ 后
python3 scripts/intake_pdfs.py --remove
# → 给新条目补 state/classification.json 一条 → 然后：
python3 scripts/apply_to_zotero.py
python3 scripts/sync_classification.py
python3 scripts/mineru_batch.py all 3
python3 scripts/summarize_batch.py 3
python3 scripts/link_markdown.py

# 收尾必跑
python3 scripts/tidy.py --apply

# 新文献入库后刷新库内「推荐分」（写进 Zotero 的 Extra 字段）
# 每日定时任务里也会自动跑一次，所以手动跑只为「入库后立刻看到分数」
python3 scripts/score_library.py --apply

# 日报（工作点都在 config/runtime.json，裸跑即可）
python3 scripts/daily_digest.py --show-config                # 先看生效参数
python3 scripts/daily_digest.py                              # 按配置出一期
python3 scripts/daily_digest.py --date 2026-01-01 --no-llm   # 回填某天，不调 LLM
python3 scripts/daily_digest.py --dry-run                    # 预演，不写文件不调 LLM
python3 scripts/daily_digest.py --force                      # 忽略频率限制，强制出一期
```

**辅助脚本**（按需）：

```bash
python3 scripts/build_profile.py          # 刷新画像（纯统计，零 API 花销）
python3 scripts/library_index.py          # 库内 DOI 索引（权威）
python3 scripts/citation_graph.py         # 引文图（增量；--rebuild 全量）
python3 scripts/journal_if.py             # 期刊 IF 表（增量；--refresh 全量重查）
python3 scripts/fill_meta.py --apply      # 去 CrossRef 补全条目元数据（只补空字段）
python3 scripts/sync_properties.py --apply  # 刷新 summary.md 的属性区
python3 scripts/embed_figures.py 4        # 给 summary.md 配 3–5 张关键图
python3 scripts/fix_control_bytes.py --apply  # 修坏字体的 NUL 字节
python3 scripts/tidy.py                   # 只报告（默认）；--apply 才动手
```

**`tidy.py` 会删东西**，先看默认的报告再 `--apply`：

| 类别   | 判定                               | 处理                                          |
| ---- | -------------------------------- | ------------------------------------------- |
| 空条目  | 顶层条目没有任何子项（无附件、无笔记）              | 硬删除（**已判定为重复条目待删的除外**，它走合并那条路）              |
| 重复条目 | DOI 或标题归一化后相同                    | 合并后硬删除；**有子项的重跑默认只报告**，加 `--merge-dups` 才合并 |
| 孤儿目录 | `papers/<key>_*` 在 Zotero 已无对应条目 | **移动**到 `state/trash/<日期>/`（可逆，不是删除）        |
| 空目录  | 没有任何条目归属**且没有子目录**的分类目录          | 硬删除（只删叶子）                                   |

> ⚠️ `DELETE /items/<key>` 是**硬删除，不进回收站**（§7.7 有核验方法）。
> 所以这些操作必须先备份、默认只报告。
> `tidy.py` 只删**叶子**目录（删父目录会连带删光子目录），并跳过 `archive_collections` 子树
> 与 `extra_categories`。`--apply` 前会把要删的东西全量导出到 `state/tidy_backup_<日期>.json`，
> 并把 `classification.json` / `manifest.json` 里指向已消失条目的记录一并清掉；
> 跑完再执行 `sync_classification.py && digest.py json` 刷新总表与摘要清单。

### 5.4 手工撰写 `state/classification.json`

这是唯一必须人工撰写的核心数据。格式（真实样例）：

```json
[
 {
  "key": "ABCD1234",
  "dir": "papers/ABCD1234_Example_paper_title_in_snake_case",
  "title": "Example paper title (与 Zotero 条目一致)",
  "year": "2024",
  "primary": "界面反应",
  "secondary": "结构退化",
  "sub": ["电解液分解"],
  "is_review": false,
  "method": ["实验-电化学", "实验-表征"],
  "materials": ["NCM/NCA", "电解质/界面"],
  "form": ["多晶二次颗粒"],
  "confidence": "high",
  "note": "给你自己看的一句话备注，脚本不用它"
 }
]
```

- `key` 从 Zotero 拿；`primary` 必须是 `category_order` 里的一级分类；
  `sub/method/materials/form/system` 是标签轴的取值。
- `form`（`形态:`）是**形貌**、`system`（`体系:`）是**电解质体系**——都不属于材料体系，
  都只打标签、不建目录；`system` 只写非默认情形（固态 / 固液对比），液态留空。
- **一级分类单值**，但条目可同时挂多个一级目录——`secondary` 会一并写入。
- `confidence`、`note` 只供你自查；`coupling` 可选。
- ⚠️ **`state/classification.json` 缺失会让 `daily_digest.py` 直接崩**
  （`daily_digest.py:823` 无 try 保护）。其余缺文件基本都能优雅降级：

| 缺什么 | 后果 |
|---|---|
| `state/profile.json` | 回退用 `find_new.QUERIES`，并提示先跑 `build_profile.py` |
| `state/library_index.json` | DOI 去重退化成标题匹配 |
| `state/citation_graph.json` | **引文推荐通道整条跳过** |
| `state/journal_if.json` | `J'` 全按 0（中性） |
| `state/recommendations.json` | 从空开始（旧的 `digest_seen.json` 会自动迁移） |
| `state/work/lib_emb.npz` | `S` 项跳过，0.15 权重回补给 `R` |
| `state/classification.json` | **崩溃退出** |

---

## 6. 参数详解

### 6.1 `config/runtime.json` 五段总览

**「日报怎么跑」集中在这一个文件里**，与领域内容分家（§4.3 讲了两者的分工）。
装载器是 `scripts/runtime.py`，路径可用环境变量 `LITHUB_RUNTIME` 覆盖。

| 段 | 键 | 作用 |
|---|---|---|
| `schedule` | `enabled` / `every_n_days` | 跑不跑、隔几天跑一期（见 §9.2 的频率闸门） |
| `digest` | `limit` / `days` / `min_year` / `cooldown_days` / `min_score` / `cat_floor` / `oa_top` / `model` | 版面篇数与各种阈值 |
| `pool` | `refresh_days` / `rows` / `pages` / `query_pages` | 检索池的刷新周期与取数深度 |
| `scoring` | `w_r` … `c_age_tau_y` | 打分权重与各分量常数（§6.3） |
| `email` | `enabled` / `to` / `smtp_host` / `smtp_port` / `smtp_user` / `use_ssl` / `subject` | 日报发信（见 §6.6.8）。**密码不在这里**——读 `state/smtp_password` |

优先级 **CLI 参数 > `runtime.json` > 内置默认**；内置默认与原先的硬编码**逐字一致**，
所以配置文件缺失时行为不变（`runtime.py` 会把 `LOADED=False` 报出来）。
查生效值：`python3 scripts/daily_digest.py --show-config`（或 `python3 scripts/runtime.py`）。

### 6.2 逐字段说明与调参经验

> 「内置默认」= 代码里 `runtime.py` 的 `DEFAULTS`（配置缺失时用它）；
> 「本包现值」= 仓库里 `config/runtime.json` 当前写的值，**这是活配置，不是模板**。
> 每段开头的 `_help` 是文件里写给人的字段说明，装载器当注释读掉。

#### 6.2.1 `schedule`

| 键 | 内置默认 | 本包现值 | 说明 |
|---|---|---|---|
| `enabled` | `true` | `true` | 跑不跑。systemd timer 照旧每天触发，由这里决定这一期推不推；false = 不推（日志里会写明原因，不会静默） |
| `every_n_days` | `1` | `1` | 隔几天跑一期：1 = 每天；>1 = 距上次成功出报不足 N 天就跳过。上次出报的日期记在 `state/last_push.json`。想立刻跑一期用 `--force` |

#### 6.2.2 `digest`

| 键 | 内置默认 | 本包现值 | 说明 |
|---|---|---|---|
| `limit` | `16` | `12` | 每期日报的篇数，建议 10–20。token 花费与耗时都近似线性：约 **0.0014 元/篇**；实测 48 篇的 dry-run 约 8 分钟、16 篇约 4 分钟（均已命中缓存） |
| `days` | `14` | `30` | fresh 通道的回看窗口（天），按 Crossref 的创建日期算。调大能多看到些刚上线的论文，但窗口重叠部分只多花网络、不多花 token |
| `min_year` | `0` | `0` | query 通道的年份下限，0 = 不限年份。别改回 2010 之类的值——那会把 1980 年的 LiCoO₂ 这类经典挡在闸门外，与公式里「经典不因年龄掉权重」的设计冲突 |
| `cooldown_days` | `60` | `60` | 同一 DOI 多久内不再推（天）。调大会减少重复，但备货（合格候选池）消耗更快 |
| `min_score` | `0.04` | `0` | 入选硬门槛：final 分数低于它的候选任何通道都不入选。⚠️ 改过 `scoring` 段之后**必须重新标定它**；0 = 关掉门槛 |
| `cat_floor` | `""`（空串） | `力学耦合=2,界面反应=1` | 分类保底席位，「分类=整数」逗号分隔，空串 = 关闭保底。**分类名必须存在于 `topic.json` 的 `category_order`**，否则脚本启动即报错退出 |
| `oa_top` | `48` | `60` | 定 `A`（是否开放获取）时，除各通道短名单外，再按不含 `A` 的分取全局前 N 名去查 Unpaywall。0 = 只查短名单 |
| `model` | `deepseek-v4-flash` | `deepseek-v4-flash` | 翻译标题 / 摘要 / 提要用的模型 |

#### 6.2.3 `pool`

| 键 | 内置默认 | 本包现值 | 说明 |
|---|---|---|---|
| `refresh_days` | `30` | `30` | query / refs 两个通道的候选池多久刷新一次（天）。缓存键按 N 天切一个周期桶，同一周期内重跑零网络，跨周期自动重取。⚠️ 改这个值会让缓存键整体失效，下一次这两个通道要冷一次：实测整期约 **28 分钟**（平时命中缓存约 10 分钟） |
| `rows` | `100` | `300` | Crossref 每页条数。fresh 每期必冷的请求数是「检索式数 × pages」，与 `rows` 无关，所以 `rows` 大一些基本是白赚候选（一次 100 条约 **7.9 秒**、50 条约 3.0 秒，耗时随 rows 涨） |
| `pages` | `1` | `7` | fresh 通道的页数。fresh 每期必冷，页数是每日耗时的主因，默认 1 |
| `query_pages` | `3` | `3` | query 通道的页数，独立于 `pages`。query 命中周期缓存，所以多翻页只在跨周期那一次花网络 |

#### 6.2.4 `scoring`（权重与分量常数）

6 个权重与 9 个分量常数，**一起标定**；含义与公式见 §6.3。

| 键 | 内置默认 | 本包现值 | 含义 |
|---|---|---|---|
| `w_r` | `0.40` | `0.40` | 检索排名 `R` 的权重。有 `S` 时 `R` 实际拿 `w_r − w_s`（那一份分给 `S`）；没有 `S` 时整份 `w_r` 都归 `R` |
| `w_s` | `0.15` | `0.20` | 库内语义相似度 `S` 的权重，从 `w_r` 里分出来。需要 `sentence-transformers` 与 `state/work/lib_emb.npz`，缺任一项 `S` 就整项跳过、这一份回补给 `R` |
| `w_c` | `0.22` | `0.20` | 库内共被引 `C` 的权重。**分类权重（`topic.json` 的 `pref`）只在这里生效** |
| `w_x` | `0.20` | `0.15` | 经典度 `X` 的权重——只看绝对被引量，完全不看年份 |
| `w_j` | `0.13` | `0.10` | 期刊层级 `J'` 的权重，IF 查 `state/journal_if.json` |
| `w_a` | `0.05` | `0.01` | 开放获取 `A` 的权重（1/0） |
| `f_gain` | `0.35` | `0.40` | 新鲜度加成上限：`final = B × (1 + f_gain × F)`。`F` 只加成不惩罚，老文 ×1.0、新文最多 ×(1+f_gain) |
| `f_half_life` | `180.0` | `180.0` | `F = exp(−Δ/f_half_life)` 的天数常数，Δ 按 published 日期算（不用 Crossref 创建日期） |
| `x_sat` | `5001.0` | `2000.0` | `X` 的饱和被引数：`X = ln(1+被引数) / ln(x_sat)`，到这个数就顶到 1 |
| `j_if_zero` | `4.0` | `6.0` | `J'` 的零点 IF：IF 等于它的中位刊不奖不罚 |
| `j_if_top` | `32.0` | `30.0` | `J'` 的饱和 IF。末尾那个减 0.5 不能省，否则所有白名单刊都白拿 0.1–0.15 |
| `c_n_sat` | `31.0` | `30.0` | `C` 里引用篇数的饱和点：`ln(1+n) / ln(c_n_sat)` |
| `c_w_floor` | `0.65` | `0.65` | `C` 里「引用者分类权重」项的地板：`(c_w_floor + (1−c_w_floor)·q̄)` |
| `c_age_floor` | `0.75` | `0.75` | `C` 里「引用者年龄」项的地板：`(c_age_floor + (1−c_age_floor)·r̄)`。调到 0.9 就更彻底地忽略引用者年代 |
| `c_age_tau_y` | `3.0` | `3.0` | `C` 里引用者年龄的衰减常数（年）：`r̄ = exp(−Δy / c_age_tau_y)` |

> ⚠️ **改 `scoring` 就必须重标定 `--min-score`**：那 6 个权重是一起标定的，动一个门槛就失配。
> 校验上只有两条不变量给**警告**而非报错：`w_r+w_c+w_x+w_j+w_a ≈ 1.0` 与 `w_s ≤ w_r`
> ——因为改了它们会让 `--min-score` 的刻度跟着变，属于「有救但必须让人看见」。

#### 6.2.5 `email`

| 键 | 内置默认 | 本包现值 | 说明 |
|---|---|---|---|
| `enabled` | `false` | `true` | 发不发日报。false = `send_digest.py` 直接跳过（日志写明原因，不算失败） |
| `to` | `""` | 你的邮箱 | 收件人，多个用逗号分隔 |
| `smtp_host` | `""` | `stu.xjtu.edu.cn` | ⚠️ XJTU 的证书是通配符 `*.xjtu.edu.cn`，匹配 `stu.xjtu.edu.cn`、**不匹配** `smtp.stu.xjtu.edu.cn`（后者多一层标签，握手报 `Hostname mismatch`）。两个名字是同一台机器（202.117.1.52）。别改回 `smtp.stu.…`，否则 TLS 校验必失败 |
| `smtp_port` | `465` | `465` | 465 = SSL（XJTU 只开了 465 与 25；587 未开放） |
| `smtp_user` | `""` | 完整邮箱地址 | 登录账号。注意：发信服务器用 `stu.xjtu.edu.cn`，账号仍写 `@stu.xjtu.edu.cn` 全称 |
| `use_ssl` | `true` | `true` | true = 465 端口的 SSL 直连；false = 明文连接后 STARTTLS（用于 587） |
| `subject` | `文献日报 {date}（{n} 篇）` | 同左 | 主题模板，可用 `{date}` 与 `{n}`（篇数）。**模板化、不调 LLM，所以发信本身零 token 花费** |

更多发信的坑（专用密码 ≠ 登录密码）见 §6.6.8。

### 6.3 日报打分公式与三个通道

#### 6.3.1 三个通道 + 三轮发牌

日报按**三个通道**选文献：各自过闸后统一打分，版面**分三轮发**：

1. **通道配额轮**：fresh 40% / refs 25% / 其余 query，某通道凑不满就把配额让给别的通道；
2. **分类保底轮**：`--cat-floor` 给指定分类留保底名额，按分类权重从高到低发
   （权重高的方向先占位）；
3. **全局补位轮**：剩下的名额给全局分数最高的候选（每类上限 `cap = limit 的 40%`
   只在**这一轮**可以被放宽）。

第 1 轮的比例是**扣掉保底预留后的 budget** 算的，不是按 `limit`——否则三通道配额之和
仍是 `limit`，第一轮就成了先到先得，播放顺序里排最后的 refs 可能一席都拿不到、
只能靠补位轮找回，配额的通道多样性作用等于没了（预留越大偏差越明显）。
保底名额不得超过 `cap`，合计不得 ≥ `limit`（超了按比例缩到 `limit//2` 并告警）。

**为什么需要保底**（2026-09-16）：`R` 去掉内容分类权重后（见 §6.3.4），分类权重不再影响
fresh/query 通道的择优，实测入选里力学耦合从 3 跌到 1（它入池只有 91 篇，结构退化 717 篇）。
保底把 `w_cat` 原来的「补偿供给不足」职能接了过来，同时不打分、不引入三重加权。
**保底复用 `take()`，所以 `--min-score` 与每类 `cap` 照旧管着它**——保底给的是「版面名额」
而不是「免检入场券」，跨不过门槛就空着，并在日志里写明原因（候选不足 / 被门槛挡下 /
撞到 cap / limit 已发完）。日志里「保底席位：力学耦合 3/3、界面反应 2/2」一行
是复核用的，计数取保底轮结束时点（之后的补位轮可能再给同一分类加人，那不算保底的功劳）。

实测（2026-09-16 同一批缓存，limit 16）：去 `w_cat` 后为「离子输运 4、结构退化 4、氧活性 3、
界面反应 2、基础理论 2、力学耦合 1」；加保底后为「离子输运 3、氧活性 3、结构退化 3、
力学耦合 3、界面反应 2、基础理论 2」，保底两项均达标。

#### 6.3.2 打分公式

```
B = 0.25*R + 0.15*S + 0.22*C + 0.20*X + 0.13*J' + 0.05*A    # S 可用时
B = 0.40*R + 0.22*C + 0.20*X + 0.13*J' + 0.05*A             # S 不可用：那 0.15 回补给 R
final = B * (1 + 0.35*F)                     # 综述再 ×0.9
```

系数写死在这里的是**内置默认**；配置改过 `scoring` 段就按配置的算（§6.2.4）。

#### 6.3.3 六项的含义

| 项 | 含义 |
|---|---|
| `R` | **纯检索排名** `1/(1+rank/5)`。2026-09-16 起**不再乘内容分类权重**，理由与实测代价见 §6.3.4 |
| `S` | 候选与**库内 147 篇**的最大余弦相似度（候选 `title+abstract` 对库内 `title+concl+method`）。需要 `state/work/lib_emb.npz`（`scripts/build_embeddings.py` 生成）与 `sentence-transformers`，缺任一项就整项跳过、把那 0.15 回补给 `R`，并在日志写明原因 |
| `C` | 库内共被引：`min(1, ln(1+n)/ln(31))·(0.65+0.35·q̄)·(0.75+0.25·r̄)`；`n`=被几篇库内文献引用，`q̄`=引用者平均分类权重比（**分类权重现在只在这里生效**），`r̄`=引用者平均 `exp(-Δy/3)`（Δy 单位**年**） |
| `X` | 经典度：`min(1, ln(1+被引数)/ln(5001))`——只看绝对被引量，**完全不看年份**（Crossref `is-referenced-by-count`，三个通道都取） |
| `J'` | 期刊层级：`clamp(ln(IF/4)/ln(8), 0, 1) − 0.5`，IF 查 `state/journal_if.json`（OpenAlex 代理指标，**不是 JCR**）；查不到按 0（中性） |
| `A` | 开放获取（1/0） |
| `F` | 新鲜度 `exp(-Δ/180天)`，只加成：老文 ×1.0、新文最多 ×1.35（用 published，不用 Crossref created） |

**年龄不再做乘法惩罚**，这是这套公式的核心：`X` 不看年份，`C` 里引用者年代只做 ≤25% 的
有界调节（地板 `--citer-floor`，默认 0.75）。旧公式 `C = Σ exp(-age/730天)/4` 让 10 年前的
引用只剩 **0.7%** 权重，等于把「被多少篇库内文献引用」退化成年龄惩罚——1980 年的 LiCoO₂
开山作因此在经典组里垫底。**`J'` 减 0.5 也不能省**：直接留 `[0,1]` 会让所有白名单期刊
白拿 0.1–0.15，总分整体抬高、门槛失去意义；减完是「好刊加分、普通刊小扣」，净效应接近零。

`A` 只在**有资格入选的候选**里取值一致：先按不含 A 的分排短名单（各通道配额+2）与全局前
`--oa-top`（默认 48）名，只有这批去查 Unpaywall 拿 `A`，其余 `A=0`。旧代码只给短名单查 OA，
却拿含 `A` 的分做全局排序与门槛判定——进短名单的白拿 0.15、没进的 1300 篇候选被静默扣分
（它们从没被查过），那是 bug 不是设计。

#### 6.3.4 `R` 为什么与分类解耦

旧 `R = (1/(1+rank/5))·(w(cls.primary)/w_max)`。`rank` 本身已经来自画像生成的检索式，
再乘一次内容分类权重是**三重加权**（检索式条数 + rank + w_cat），且 `cls.primary` 是关键词
数出来的、与命中通道 `hit_cat` 一致率只有 **30%**（1362 篇候选里 413 篇），
等于用一个有噪声的量去修正另一个。改完之后 `R` 只回答「搜索引擎认为它匹配到什么程度」，
画像的作用交给检索式（供给）与 `S`（打分）。

**代价是实测出来的**（同一批缓存数据，2026-09-16）：去掉 `w_cat` 后入选
`力学耦合 3→1`、`基础理论 0→2`、`离子输运 2→4`、`结构退化 6→4`，通道上
`refs 8→4`、`query 6→9`，低于门槛的候选 236→177。也就是说 `w_cat` 真正在干的是
**补偿供给不足**（力学耦合入池仅 91 篇，结构退化 717 篇）：它的「打分」职能删对了，
「保底」职能还空着（已由 §6.3.1 的分类保底补上）。

**分类权重（`topic.json` 的 `categories.<类>.pref`，`build_profile.py` 里是 `CAT_PREF`）
现在只剩一处生效**：`C` 的 `q̄`（「库内权重高的方向引用了它」，是画像信号，不算冗余），
**不再影响 `R`**，也影响不到 fresh/query 通道的择优。调法不变：改 `build_profile.py` 顶部的
`CAT_PREF`（自动权重 × 偏好，上限 3.0），`python3 scripts/build_profile.py` 重跑；
**不要手改 `state/profile.json`**（`lithub-profile.timer` 每月会覆盖它）。
`文献画像.md` 第 1 节打印的 `w/w_max` 现在只喂 `q̄`。

另外两点要注意：

- **refs 通道没有排名，`R=0`**，分类权重只能经 `C` 的 `q̄` 起作用。力学候选里有
  **30/96** 走这个通道。
- ⚠️ **启用 `S` 前必须重标定 `--min-score`**：有 `S` 时几乎每篇候选都拿 `0.15×S`，
  实测（stub 向量）门槛挡下数从 **177/1426** 掉到 **13/1426**；`0.04` 是**无 `S` 时**标定的。

#### 6.3.5 三个通道的取数方式

| 通道 | 来源 | 取数参数 |
|---|---|---|
| fresh | Crossref 近 `--days` 天新入库的 journal-article，**窗口固定，不再自动放宽**；缓存键含 `since`，每期必冷 40 个请求（检索式数 × 页数） | `--rows` 100 × `--pages` 1 |
| query | 同一检索式但不加日期过滤，按相关度分页多取；**默认不限年份**（`--min-year 0`。旧默认 2010 会把 1980 年的 LiCoO₂、1996 年的 PBE 挡在闸门外，与「经典不因年龄掉权重」冲突）；缓存键带**刷新周期桶**（`period_tag`，默认 30 天），同一周期内重跑零网络、跨周期自动刷新 | `--rows` 100 × `--query-pages` 3 |
| refs | 库内引文推荐：`state/citation_graph.json` 的 `co_citation`。先用 `ref_meta` 的期刊/标题做零网络初筛（参考文献刊名是 ISO 缩写，先展开成白名单全名再判），剩下的才按 DOI 批量取元数据（批次键同样带周期桶）。**被引数取 Crossref 的 `is-referenced-by-count`**，`X` 靠它 | — |

#### 6.3.6 工作点与门槛的实测标定

工作点 `--rows 100 --pages 1 --query-pages 3 --min-score 0.04` 是实测定的
（现在都在 `config/runtime.json` 的 `pool` / `digest` 段）：单请求 100 条约 **7.9 s**、
50 条约 **3.0 s**（耗时随 rows 涨），但 fresh 每期必冷的请求数是固定的 40 个，
100 条换来的候选是 50 条的**两倍**（16 篇 vs 7 篇），所以宁可要 100 条——
每日稳定态实测约 **10 分钟**（2026-09-20，40 条检索式、query/refs 命中周期缓存），
仍在预算内。

`--min-score`（内置默认 0.04）是硬门槛：final 低于它的候选任何通道都不入选，
配额空出的名额让给全局最高分的其他候选。门槛随公式一起重标定过（`R` 权重 0.55→0.40、
`A` 0.15→0.05 让相关度主导的分数整体下移，`X` 与 `J'` 又把经典与好刊抬回去）：
0.04 挡掉实测 `final ≲ 0.035` 的噪声（R≈0.02、0 被引、普通刊），但不误杀 fresh 版面那一档
（默认 limit 下最低入选实测 **0.076**）。

`--citer-years`（默认 3 年）与 `--citer-floor`（默认 0.75）是 `C` 里引用者年龄的衰减常数与
地板——地板调到 0.85–0.90 就更彻底地忽略引用者年代；旧的 `--citer-tau`（天）已随公式重写移除。
`--oa-top`（默认 48）是定 `A` 时除短名单外额外查 Unpaywall 的全局名额，0 = 只查短名单（旧行为）。

调完用 `python3 scripts/daily_digest.py --dry-run --diag-cats --date <日期>` 看效果：
每类的候选数量、命中来源（哪条检索式捞到的）、以及最高分的几篇离入选线差多少。
实测（2026-09-15，库内 118 篇）：只把力学耦合乘到 2.5 倍时，力学候选 85 篇里只有
19 篇来自力学检索式，最高分 0.326 仍低于入选线（当日入选 力学耦合 1 篇）——
**加权重换不来力学文献，必须同时加检索式**。补了 4 条检索式（力学耦合 6→8、
界面反应 7→8）后，力学候选 96 篇、其中 35 篇来自力学检索式，最高分升到 0.481 和
0.445（后者是某条检索式的第 1 名），入选变成 力学耦合 3、界面反应 6（顶到上限）。
选检索式的办法：直接量产率——`search_crossref` 拉 100 条，过 `journal_ok` +
`LAYERED_PAT` + `relevant()` 后再 `classify()`，数「过闸篇数 / 其中力学耦合篇数 /
最好排名」。实测 `chemo-mechanical degradation Ni-rich cathode` 一次给 10 篇力学耦合，
而 `stress corrosion cracking cathode electrolyte cathode` 给 0 篇。

**想让力学耦合回到每天 3 篇左右，有两条路**（作者侧**都还没做**；分类保底后来已实现）：

1. **加分类保底席位**：在入选循环里给每个分类留保底名额，把分类权重从「分值」改成「配额」——
   既保住去三重加权，又保住手动杠杆（推荐）。
2. **补检索式**：加权重换不来力学文献，必须同时加检索式。

### 6.4 `daily_digest.py` 的 CLI 参数

```bash
# 先看生效的运行配置（config/runtime.json 再被 CLI 覆盖后的值）
python3 scripts/daily_digest.py --show-config

# 手动生成某天的日报（幂等，重跑命中缓存则零 LLM 花销）
python3 scripts/daily_digest.py                                   # 裸跑 = 用 runtime.json 的工作点
python3 scripts/daily_digest.py --date 2026-09-12 --no-llm       # 完全不调用 LLM
python3 scripts/daily_digest.py --dry-run                         # 预演，不写文件也不调 LLM
python3 scripts/daily_digest.py --dry-run --diag-pagination       # 诊断 rows/页数够不够
python3 scripts/daily_digest.py --force                           # 忽略 schedule 频率限制，强制出一期
python3 scripts/daily_digest.py --cooldown-days 30 --min-year 2015   # 显式恢复年份下限
python3 scripts/daily_digest.py --min-score 0 --citer-years 1.5      # 关门槛 / 收紧引用者年龄衰减
```

| 参数 | 默认 | 说明 |
|---|---|---|
| `--days` | `runtime.json` 的 `digest.days`（内置 14） | fresh 通道窗口天数（按 Crossref 创建日期） |
| `--limit` | `digest.limit`（内置 16） | 日报篇数，建议 10–20 |
| `--min-year` | `digest.min_year`（内置 0 = 不限） | query 通道年份下限。旧默认 2010 会把 1980 年的 LiCoO₂、1996 年的 PBE 直接挡在闸门外 |
| `--cooldown-days` | `digest.cooldown_days`（60） | 同一 DOI 的再推冷却期（天），超期后高分老文可再推一次 |
| `--rows` | `pool.rows`（100） | Crossref 每页条数。100 条约 7.9 s、50 条约 3.0 s；fresh 每期必冷的请求数是「检索式数 × pages」，与 rows 无关，所以 rows 大基本是白赚候选 |
| `--pages` | `pool.pages`（1） | fresh 通道页数。fresh 每期必冷，页数是耗时的主因 |
| `--query-pages` | `pool.query_pages`（3） | query 通道页数，独立于 `--pages` |
| `--min-score` | `digest.min_score`（0.04） | final 低于此值的候选不得入选（0 关闭门槛）。标定依据见 §6.3.6 |
| `--cat-floor` | `digest.cat_floor`（空串） | 分类保底席位，「分类=整数」逗号分隔，空串关闭。在通道配额轮之后、全局补位轮之前按分类权重从高到低发放，同样要过 `--min-score` 与每类上限 `cap` |
| `--citer-years` | `scoring.c_age_tau_y`（3.0） | `C` 里引用者年龄衰减的时间常数（**年**）——只做有界调节。旧参数 `--citer-tau`（天）已随公式重写移除 |
| `--citer-floor` | `scoring.c_age_floor`（0.75） | `C` 里引用者年龄项的地板：年龄最多只影响 25% 的权重，调到 0.85–0.90 就更彻底地忽略引用者年代 |
| `--oa-top` | `digest.oa_top`（48） | 除各通道短名单外，再按不含 `A` 的分取全局前 N 名查 Unpaywall。0 = 只查短名单 |
| `--diag-pagination` | 关 | 只做一次分页对比诊断并打印，不影响正常流程 |
| `--diag-cats` | 关 | 打印各分类候选的数量、命中来源与最高分（调检索权重时用） |
| `--no-llm` | 关 | 完全不调用 LLM |
| `--model` | `digest.model` | 翻译用的模型 |
| `--date` | 今天 | 指定日期 |
| `--dry-run` | 关 | 只打印，不写文件，且**自动开启 `--no-llm`**（dry run 不该产生任何花费）。也不更新 `state/last_push.json`，不写任何 state 文件（网络缓存 `state/work/` 除外） |
| `--force` | 关 | 忽略 `schedule` 的频率限制，强制执行一次 |
| `--show-config` | 关 | 打印生效的运行配置后退出（只拼内存里的配置，不碰网络也不写文件） |

**日报版面**：标题下直接进文献（每条给中文标题、**推荐性分数**＝final 总分，
不给 R/C/X/J'/A/F 细则；其后是来源、期刊、作者、**单位**、分类、链接、**概述**
（90–150 字中文提要，覆盖方法核心 / 主要贡献 / 关键结果，与翻译同一个请求产出）、
中文摘要、**库内关联**），然后是下载清单；检索口径、收录命中与概览表一律挪到最后
「统计口径」一节。

**成本**：检索/摘要/OA 链接全走免费接口；只有翻译调 LLM（deepseek-v4-flash，关思考链），
结果按 DOI 缓存，重跑零花销。按左一档模型（空闲时段 输入 1 元/M、输出 4 元/M）实测：
旧版一次请求翻 16 篇约 **1.8 分/天**；分块 + 中文概述后 输入 3.75K / 输出 4.17K
≈ **2.0 分/天**。

**翻译分块 + 重试**：`TRANS_CHUNK`（默认 4）篇一块、每块最多 `TRANS_RETRY`（默认 3）次重试、
每块成功即落盘。旧版一次请求翻 16 篇，2026-09-15 实测「HTTP 200 但响应体没有 choices」
→ 整期日报静默退回英文标题；分块后爆炸半径降到 4 篇。翻译不完整时日报标题下会加一行 ⚠️，
明细在 `logs/daily_digest.log` 的「翻译第 i/n 块」行。

**摘要来源是三跳**（`fill_abstract()`），前两跳落空才走第三跳：

1. Crossref `/works/<doi>` 的 `abstract`
2. Semantic Scholar 的 `abstract`
3. **出版社 landing page 的 meta 标签**（`landing_absolute()`，2026-09-20 加）——
   只对 `LANDING_ABS_PREFIX` 白名单前缀尝试，目前是 `10.1038/`（Nature 系）。
   **别扩到 Elsevier**：`sciencedirect.com` 直连 403，只为已知会被挡的出版方白花请求。
   结果（含「试过但没抠到」）缓存在 `state/work/cache_landing/`，重跑零网络。

实测 2026-09-20：近期 6 期日报 160 条里 **49 条取不到摘要（约 30%）**，且**与新旧无关、
按期刊一刀切**——Elsevier 付费刊与 Nature 系付费刊基本取不到，Wiley / RSC /
Nature Communications（OA）全部 0 缺失。免费聚合源帮不上忙（OpenAlex 1/12、
Europe PMC 0/12）；白名单那一跳把 Nature 系 **10/10** 全补上了。
剩下的 Elsevier 付费刊只能等收编后从 PDF 拿（`paper.md` 里有完整摘要）。

**单位**取自 Crossref 的 `author[].affiliation[].name`（`author` 本来就在
`CROSSREF_SELECT` 里，**零新增网络请求**），经 `clean_affil()` 规则链清洗：去 URL / 邮箱 /
ORCID / 纯数字片段 → 跨行连字符合并 → HTML 实体解码 → CamelCase 切开（**含数字的片段
一律不切**，这是保住 `LiFePO4`/`NCM811` 的唯一办法）→ 18+ 字母粘连串能切就切、切不开丢片段
→ 忽略大小写与空白去重 → `; ` 连接。**没有可用单位就整行不输出**（不写 hermes 那句
「未找到单位信息」兜底——日报每篇都跟一句是噪声）。实测 2026-09-16 那期 16 篇里 5 篇有，
其余是出版方本身没 deposit 单位，不是提取失败。

**库内关联**（`lib_links()`）零 API 花销：有引用边就写「被库内 N 篇引用（分类分布）」
＋同类的引用者篇名，没有引用边就退回「与库内 N 篇标签重合」＋最相关的 3 篇。
只转述本地事实（`citation_graph.json` 的 `co_citation` + `classification.json`），
不推断内容关系。注意：**只有 refs 通道的 `C` 参与打分**，query 通道那篇即使被
库内 13 篇引用也拿不到 `C`——版面会显示这个关联，但分数里没有它。

---

### 6.5 `summarize_batch.py` 的 CLI 参数与省钱开关

```bash
cd ~/LitHub

# 逐篇生成中文总结（幂等；属性区由 Zotero 元数据写入）
python3 scripts/summarize_batch.py 3              # 3 = 并发数

# 只跑某几篇
python3 scripts/summarize_batch.py 2 KEY1,KEY2

# 只打印最终提示词，不调模型、不写文件（改了 prompts/summarize.md 后先用它自检）
python3 scripts/summarize_batch.py --dry-run 1 ABCD1234

# 换模板 / 限制每篇最多读几张图（默认 5）
python3 scripts/summarize_batch.py --template prompts/summarize.md --max-figures 3 2

# 步数上限（默认 5，0 = 不限）：这是**省钱的主要开关**
python3 scripts/summarize_batch.py 4 --max-steps 5

# 强制重写（含已被人工补过章节的）
python3 scripts/summarize_batch.py 3 --force
```

位置参数：第一个是并发数（默认 3），第二个是逗号分隔的 key 列表（只跑这些）。
带值的开关支持 `--opt v` 与 `--opt=v` 两种写法：`--template` / `--max-figures` /
`--max-steps` / `--agent-file`。

#### 6.5.1 省 token 的四个开关（按性价比排序，2026-09-20 实测）

**① 一次调用（oneshot，默认已开，省最多）**：不让模型读文件、写文件——脚本把**裁剪后的
论文全文**直接塞进提示词，模型一次回复就是总结正文，脚本再落盘
（`--output-format stream-json` 解析 assistant 文本）。实测 **0.14 → 0.037 元/篇（省 74%）**，
只要 1 次调用。

三种载体由脚本自动选（`build_prompt()` 的 `mode`）：

| 载体 | 何时用 | 形态 | 实测 |
|---|---|---|---|
| `inline` | 裁剪后全文 ≤ 110 KB | 全文注入提示词，`prompts/oneshot.agent.md`（`tools: []`），1 次调用 | **0.037 元** |
| `file` | 塞不下（**Linux 单参数上限 128 KB**，实测 16/184 篇） | 全文写成本目录 `paper.summarize.md`，`prompts/oneshot-read.agent.md`（`tools: [Read]`），读完即答，**用完在 `finally` 里删掉** | 约 0.04 元 |
| `tool` | 前两者失败时兜底；`--no-oneshot` 可强制 | 模型自己读 `paper.md`、自己写 `summary.md`（老路径） | 0.053 元 |

**② 裁剪论文（默认已开，省约 7%）**：`prune_paper()` 去掉三块对总结没用、却占 token 的东西——
front-matter（元数据由 Zotero 给）、图片路径行（总结只写图号）、**参考文献段**。参考文献最容易
切错，两道护栏：**切完至少留原文 35%**、且切点必须落在文件 35% 之后。识别参考文献有两种办法：认
`## References` 标题（实测只覆盖 **59%**——MinerU 常把标题丢了），认不到就从末尾往回找「其后
非空行里 ≥60% 是编号引文行、且至少 15 行」的最靠前位置（不能一遇断链就停，参考文献区里常有几行
不带编号的续行，那样只能切到尾巴一小段）。合计覆盖 **165/184 篇，裁掉中位 26%（最多 67%）**。

**③ 受限 agent（`tool` 载体时生效，省约一半）**：默认会话会把**内置工具 + MCP（zotero 那 40 个）
的全部 schema** 塞进每次调用。实测只给 `Read`/`Write`（`prompts/summarizer.agent.md`），
**首调未缓存输入 35,441 → 2,429 token（省 93%）**，整篇 0.109 → 0.053 元。
`embed_figures.py` 同样接了 `prompts/figures.agent.md`。

⚠️ 两个坑：`--agent-file` 必须放在 `-p` **之前**、且用 `=` 形式——`-p` 自己吃下一个参数当提示词，
写成 `-p --agent-file X` 会让 X 变成子命令（实测报 `unknown command`）。另外 `--skills-dir`
指向空目录只省 1%，**技能不是开销大头，工具 schema 才是**。

**④ 步数上限 `--max-steps`（默认 5，只在 `tool` 载体下起作用）**：截断「反复读改」的来回。
oneshot 的 `inline`/`file` 载体没有工具调用，这个上限自然不参与。它仍然管着兜底路径，也是
「模型一步没走完」的护栏（`summarize()` 核对 `summary.md` 的 mtime，没变过就报「未落笔」）。

#### 6.5.2 提示词模板与占位符

提示词模板 `prompts/summarize.md` 有 6 个占位符：`{task}`（任务定位句，按载体换措辞）、
`{meta}`、`{library}`、`{draft_rule}`、`{source}`（论文全文，只有 inline 注入）、
`{deliver}`（交付要求），外加 FIGURES 段里的 `{figure_manifest}`。
改模板后要同步 `summarize_batch.py` 的内置兜底常量——目前是用脚本把模板正文抽出来覆写
`PROMPT` / `FIGURE_ADDENDUM` 两个常量。模板读不到就退回内置常量（缺一个文件不该让流水线停）。

#### 6.5.3 写一篇总结要花多少 token（2026-09-20 实测）

`summarize_batch.py` 逐篇跑 `kimi -p`，是**代理式写作**：模型读 `paper.md`、写 `summary.md`、
再回头改，**每一步都把整篇论文重发一遍**。所以开销几乎完全由**来回次数**决定。两个开关配合起来
最有效：`--max-steps`（截断来回）+ 提示词里的「**一回读完、一次成稿**」（让回到次数本来就少）。
提示词第 6 条要求模型用**一次** `Read`（显式带 `max_chars: 500000`）读完 `paper.md`、再用**一次**
写文件调用写完——不这样的话，275 KB 的大论文会被按行分页读，光读就花光步数配额。

| 同篇（示例） | 模型调用 | 输入 token | 输出 | 耗时 | 成本 |
|---|---:|---:|---:|---:|---:|
| 不限步数、无「一次成稿」 | 38 | 4.08 M | 56.5 k | 247 s | 0.71 元 |
| `--max-steps 8` | 8 | 0.66 M | 34.7 k | 143 s | 0.22 元 |
| `--max-steps 6` | 6 | 0.61 M | 18.6 k | 92 s | 0.23 元 |
| `--max-steps 5` + 一次成稿 | 3 | 0.21 M | 10.6 k | 52 s | 0.13 元 |
| **再加受限 agent（现在的默认）** | 3 | **0.05 M** | 6.9 k | 35 s | **0.053 元** |

30 篇实测（2026-09-20，含受限 agent）：125 次调用（均 4.2）、输入 9.71 M（未缓存 2.40 M +
缓存 7.32 M）、输出 265.8 k，**合计 ≈ 4.19 元（均 0.140 元/篇）**。这批之后套用受限 agent
同样规模约 2.0 元。成本结构：未缓存输入 57% / 输出 25% / 缓存命中 18%——**受限 agent 把
「固定开销」那一块打掉之后，剩下的主要是论文正文本身（约 17 k）与输出（约 7 k）**。

最大的一篇（`paper.md` 275 KB）也验证过：`--max-steps 5` 用 3 次调用、31 s、0.13 元；
`--max-steps 4` 用 3 次调用、41 s、0.14 元，产出都是完整七节 + 5 行表格 + `关键图表` 保留。
所以 **4–5 步是实测下限**，默认取 5。

- 那 4.08 M 输入里 **3.99 M 是重发的上下文**，钱主要花在来回上，不是花在论文上。
- 成本按 `deepseek-flash` 的 1 元/M 输入、4 元/M 输出算；**缓存命中价仓库里没有记录**，
  上面的数字按 0.1× 估（DeepSeek 官方通常是这个量级），按原价算会高 3–6 倍。
- 上限设太紧的表现不是「质量差」，而是**整篇没写**（模型把步数花在读上、还没到写）——
  所以 `summarize()` 会核对 `summary.md` 的 mtime，没被改过就报「未落笔」而不是假装成功。
- `~/.kimi-code/config.toml` 里也有全局的 `[loop_control] max_steps_per_turn`，**别用**——
  那是全进程的，会把交互式长任务的单轮步数一起限死；`--max-steps` 只作用于子进程。
- 想彻底去掉来回（一次调用 ≈ 0.05 元/篇）：把 `paper.md` 原文塞进提示词、让模型把总结当纯文本
  输出、脚本再写文件（需给 `kimi -p` 传禁用工具的 agent）。**尚未实现。**

⚠️ `--figures` 开关（让总结引用图片）**实测无效**：本环境没有视觉能力，跑 `kimi -p` 的模型
读不到 `images/` 里的图——一篇会诚实声明「未能读取图片」，另一篇则把**图注转述**写成了像是
观察结果。该开关默认关闭，**别开**。2026-09-16 起它不再让模型遍历 `images/`：脚本先抽出
「图号 + 图注」清单注入提示词，并限死最多读 `--max-figures`（默认 5）张，所以误开不再白花时间
（原先会读遍 31–246 张），但**读不到图这件事没变**。要让总结带图，做法是**把图片直接嵌进
`summary.md`**（写成 `![](images/xxx.jpg)` 相对路径，Obsidian 就地渲染），描述一律据图注与正文，
并注明未判读图像内容。细节见 §8.5、§8.6。

### 6.6 其它脚本的 CLI 参数

#### 6.6.1 `mineru_batch.py` — PDF → Markdown

```bash
python3 scripts/mineru_batch.py all 3       # all = 所有 status=="pdf" 的条目；3 = 并发
python3 scripts/mineru_batch.py <KEY> 1     # 只转某一篇
python3 scripts/mineru_batch.py all 3 --force     # 强制重转
python3 scripts/mineru_batch.py all 3 --flash     # 用免 token 的 flash 引擎（= --engine=flash）
```

- 默认引擎 `extract`（精准，需 token）；`--flash` 是兜底。
- `--force` 忽略已有产出重转。
- 引擎不同会自动重转（`paper.md` 的 front-matter 记了 `engine:`），所以 flash 转的遇到
  extract 会被重转，否则永远拿不到图。
- 并发在 `extract` + 待转 >24 篇时会被压到 ≤4。
- 成功判据：`完成：成功 N，跳过 N，失败 N`。

#### 6.6.2 `embed_figures.py` — 给 `summary.md` 配图

```bash
python3 scripts/embed_figures.py --dry-run           # 看各篇上下文大小与图数
python3 scripts/embed_figures.py 4                   # 并发 4
python3 scripts/embed_figures.py 2 KEY1,KEY2 --force
```

- 位置参数：第一个是并发数，第二个是逗号分隔的 key 列表；`--force` 重做已带「关键图表」的篇。
- 按**图注 + 正文引用句**挑 3–5 张关键图，嵌进 `summary.md` 的「关键图表」一节，图注中英双语。
  模型只做判断，**图片路径由脚本解析填入**（防它编造文件名），图号不存在则丢弃并记录。
- 只给模型标题/摘要/结论/各图图注与引用句（3–7 KB），**不喂全文**，token 约为 1/10。
- 一篇约 20–28 秒；已带「关键图表」的会跳过。
- 少数 PDF 有图但**没有图注**（实测 4/103），无法把图片对应到图号，脚本会跳过并报告。

#### 6.6.3 `sync_properties.py` — 刷新 `summary.md` 的属性区

```bash
python3 scripts/sync_properties.py              # 只报告差异（默认）
python3 scripts/sync_properties.py --apply      # 写回，改前自动备份属性区
python3 scripts/sync_properties.py --apply --key ABCD1234
python3 scripts/sync_properties.py --build-abbr # 只补期刊简写表
```

字段来源与规则见 §6.8。

#### 6.6.4 `fill_meta.py` — 补全 Zotero 条目元数据

```bash
python3 scripts/fill_meta.py            # 只报告（扫全库顶层条目）
python3 scripts/fill_meta.py --apply
python3 scripts/fill_meta.py --key ABCD1234
python3 scripts/fill_meta.py --limit 20  # 只看前 N 条
```

- 条目常缺 DOI / 刊名 / 年份 / 卷期页（全库顶层 155 条里 **65 条**有缺口，含 `issue`——
  Elsevier 系刊条目本来就常缺期号），Zotero 自己补不了，而 `cite` 字段就靠它们。
- **只补空字段，绝不覆盖已有值**；有 DOI 按 DOI 取，没有才用标题检索。
- 标题检索要求相似度 ≥0.95 **且作者重叠 ≥50%**，多条候选优先期刊论文（非预印本）；
  宁可不动，也不写可能错的 DOI。
- 作者名单只在「Zotero 名单是 CrossRef 名单的严格前缀」时补齐末尾漏掉的作者，
  顺序或中间有出入一律不动——所以有时仍要人工核。
- 写前把受影响条目的**完整旧数据**存进记档 `state/meta_fills.json`
  （追加式：日期 / key / 字段旧值→新值 / 来源 / 相似度），人工补的那次也记在这里。
- CrossRef 也查不到的（多是该刊自己就没给卷期页）会列成「CrossRef 也没有」，不是失败。
- 人工核过的例外：有一篇（Thornton 等）Zotero 名单漏了末位共同通讯作者
  Bethan J. V. Davies，而 CrossRef 那份**预印本**记录把通讯作者排在首位、顺序不同，
  前缀规则不认，只能照 PDF 首页人工补。
- 补完记得 `sync_properties.py --apply` 刷新 `summary.md` 的属性区（卷期页、DOI 会变）。

#### 6.6.5 `tidy.py` — 常规清理

```bash
python3 scripts/tidy.py                          # 只报告，默认
python3 scripts/tidy.py --apply                  # 备份后执行
python3 scripts/tidy.py --apply --keep-dups      # 不动重复条目
python3 scripts/tidy.py --apply --merge-dups     # 连有子项的重复条目一起合并
```

四类对象与细节见 §5.3。`--merge-dups` 的合并动作：把重复条目的子项挂到留存条目
（**同一份文件**——同类型同文件名——不搬，随重复条目一起删），标签/目录并过去，再删重复条目；
搬过去的 PDF 若是留存条目的 `manifest.json` 路径指向已消失文件的那一份，顺手把路径改成新位置。
留存条目由 `pick_survivor()` 挑，**优先留流水线认得的那条**（有 `papers/` 目录）——
`papers/<key>_*`、`manifest.json`、`classification.json` 都按 key 认人，留错 key 要连带改名
一堆东西（2026-09-20 修：此前只看子项/PDF，会把「只有 PDF 的重复条目」选中、把已入库那条删掉）。

#### 6.6.6 `score_library.py` — 库内文献打分

```bash
python3 scripts/score_library.py             # 只报告（默认，不写任何东西）
python3 scripts/score_library.py --apply     # 写 state/library_scores.json + Zotero 的 Extra
python3 scripts/score_library.py --key XXXXXXXX   # 看某一条的分量
python3 scripts/score_library.py --top 50    # 报告里列前几名（默认 20）
```

给**库内**每篇算一个「推荐分」，写进 Zotero 条目的 `extra` 字段，同时落在
`state/library_scores.json`（含四个分量，便于复核）。

**为什么不能照搬日报的公式**——其中两项**对库内文献没有意义**：

| 项 | 为什么不能用 |
|---|---|
| `R` 检索排名 | 库内文献不在任何检索结果集里，没有 rank |
| `S` 库内相似度 | 「与库内文献的最大相似度」对库内文献本身恒 ≈ 1，等于白送 |

所以这里把这两项**置 0**，把剩下的 `C / X / J' / A` **按原比例重新归一化到 1**，再乘
新鲜度加成。量纲与日报接近但**不是同一个数**，别拿两边直接比大小。
四个分量都**复用 `daily_digest.py` 的函数**（`citation_term` / `classic_term` /
`journal_term` / `freshness` / `final_score`），保证与日报同源、不会两套公式漂移。

`C` 是真有信号的：实测 184 篇里 **127 篇**被库内其他文献引用过，被引最多的正是
VASP / PBE / Sun 的 Ni-rich 综述这类骨架文献。实测分数区间 **-0.069 – 0.669、中位 0.30**；
**允许负分**（J' 的零点在 IF=6，低 IF 刊 + 0 被引 + 无库内引用的新文会落到 0 以下，
实测 **5/187** 篇）——没有截断，因为截断会让整个底部并列在 0、丢掉区分度。

**写进哪里**：条目的 `extra` 里一行 `推荐分: 0.312`。**`Extra` 是 Zotero 的原生列**
（条目列表表头右键 → 列 → 勾上 Extra），点表头可排序。定长三位小数是为了让
**字符串排序 = 数值排序**。同键的行**原地替换**（`upsert_line` 自己实现，
与 Zotero 的 `combineExtraFields` 同语义），所以幂等、不会堆重复行。

> ⚠️ **标签做不到这件事**：标签不是列，纯文本标签根本不会出现在条目列表里
> （只有被染色或含 emoji 的标签会以小块出现在标题格）。真正的「自定义列」需要装插件
> ——Zotero 只给插件开了 `ItemTreeManager.registerColumn`，本地 HTTP API 没有这个能力。

**两个更新钩子**（幂等，重复跑无副作用）：`lithub-daily.service` 的**第二个
`ExecStartPost`**（每天出完报顺手刷新一次，带 `-`，失败不影响出报）；
以及**收编新文献后手动跑一次** `python3 scripts/score_library.py --apply`。

零 token 花费（全程不调 LLM）。冷缓存时约 **5 分钟**（184 次 Unpaywall + 2 次 Crossref
批量）；缓存热了整轮 **7 秒**。

⚠️ **别忘 `LITHUB_MAILTO`**（§3.9）：Unpaywall 会硬拒占位邮箱，而且 `cached_json` 要先重试
退避 **~12 秒**才失败，等于每篇白等一轮、`A` 项永远拿不到值。

#### 6.6.7 `find_new.py` — 主动检索库外新文献

```bash
# 用法：python3 scripts/find_new.py [起始日期] [每查询条数] [来源]
python3 scripts/find_new.py 2025-01-01 80 crossref
python3 scripts/find_new.py 2025-01-01 25 openalex
```

- 按库内一级分类的检索式检索库外新文献（幂等，原始响应缓存在 `state/work/`）。
- 结果写 `state/candidates.json`（全量）与 `state/candidates_meta.json`；
  日志追加到 `logs/find_new.log`。
- 源可选 `crossref`（默认，覆盖广）或 `openalex`（相关度好，但有 429 限流风险）。
- 过滤链：期刊白名单 → 去重（DOI + 标题模糊）→ 体系相关性闸门 → 离题词。
- 摘要来源依次为 Crossref → Semantic Scholar，Elsevier 系多不开放摘要，会标记「未取到」。

#### 6.6.8 `send_digest.py` — 把日报发成邮件

```bash
python3 scripts/send_digest.py --dry-run        # 只打印将发送的内容概要，不连服务器
python3 scripts/send_digest.py --check          # SMTP 连通性 + 登录自检，不发送
python3 scripts/send_digest.py                  # 发当天（已发过则跳过）
python3 scripts/send_digest.py --date 2026-09-19  # 补发某天
python3 scripts/send_digest.py --force          # 强制重发（计数 +1）
python3 scripts/send_digest.py --to a@b.c       # 临时覆盖收件人（逗号分隔），测试用
```

把 `文献日报/<日期>.md` 作为**纯文本**邮件发出去（不转 HTML：邮件客户端本来也不渲染
Markdown，保持原文最忠实，也避免为一个渲染功能引入第三方库）。**零 token 花费**——
日报在 `daily_digest.py` 里已经生成好了，这里只读文件 + 说 SMTP，全程不调 LLM。
主题行用 `email.subject` 模板填 `{date}` / `{n}`。

**配置与密钥分离**：收件人 / 服务器 / 端口在 `config/runtime.json` 的 `email` 段（§6.2.5）；
**密码读 `state/smtp_password`（chmod 600）**，因为那个 json 是要进复现包给别人的，
和 `state/zotero_local_key` 同一套路。

**当日计数**：一天成功发出几次记在 `state/email_sent.json` 的 `count` 字段。两个钩子：
① `lithub-daily.service` 的 `ExecStartPost`（05:00 出完报立刻发，带 `-` 前缀，
发信失败不让这个 unit 变 failed）；② `lithub-mailcheck.timer`（06:00 兜底）。
只要计数 ≥ 1 就不再自动发，所以脚本可以随便多跑；`--force` 才会计数 +1 再发一次。

⚠️ **两处踩过的坑**：

- **XJTU 的 SMTP 主机名不能照官方文档填**。官方给的是 `smtp.stu.xjtu.edu.cn`，但它的
  证书是通配符 `*.xjtu.edu.cn`，**通配符不跨两层标签**，握手直接 `Hostname mismatch`。
  改用 **`stu.xjtu.edu.cn`**（同一台机器 202.117.1.52，证书合法、SMTP 正常）。
  别把它当笔误改回去。
- **登录密码 ≠ 客户端密码**。XJTU 邮箱的 SMTP/IMAP 只认网页端生成的**专用密码**
  （设置 → 客户端设置 → 专用密码生成），用登录密码会一律 535 认证失败——
  而且 Coremail 对「密码错」和「服务未开」返回同一个错，排查时容易走偏。
  专用密码**只在生成时可见**，丢了只能重新生成。

#### 6.6.9 其余脚本

```bash
python3 scripts/intake_pdfs.py --dry-run     # 收编待整理/里的 PDF（--remove 收编成功后删原件）
python3 scripts/build_profile.py             # 刷新文献画像（纯统计，零 API 花销）
python3 scripts/library_index.py             # -> state/library_index.json（权威 DOI 索引）
python3 scripts/citation_graph.py            # -> state/citation_graph.json（增量；--rebuild 全量重建）
python3 scripts/journal_if.py                # 期刊层级表（增量：已有 IF 的不重查）
python3 scripts/journal_if.py --refresh      # 全部重查（全量 60 余个刊 ≈ 60 个请求）
python3 scripts/fix_control_bytes.py         # 只报告
python3 scripts/fix_control_bytes.py --apply # 写入（原件备份到 state/trash/）
python3 scripts/build_embeddings.py          # 库内语义向量（S 项用）
python3 scripts/build_embeddings.py --force  # 忽略已有 npz 强制重建
python3 scripts/digest.py json               # 从 summary.md 抽取紧凑清单 -> state/digest.json
python3 scripts/apply_to_zotero.py           # 把分类与标签写回 Zotero（纯新增）
python3 scripts/sync_classification.py       # 刷新 分类与标签总表.md
python3 scripts/link_markdown.py             # 把 paper.md / summary.md 挂成 linked_file
python3 scripts/migrate_labels.py [--apply]  # 标签改名 / 删游离标签 / 原地改目录名
```

### 6.7 文献画像（`build_profile.py`）的 8 节

**文献画像的 8 节**：1 分类分布与检索配额 / 2 子类标签热度 / 3 方法・材料・形态・体系 /
4 年份与期刊 / **5 时间趋势** / **6 值得注意的发现** / **7 被引与推荐分** / 8 当前每日检索式。

后三节（2026-09-20 加）全部**由规则算出、零 token**（`build_profile.py` 不调 LLM，
可以放心挂每月定时）：

- **§5 时间趋势**：出版年 × 一级分类交叉表（近 12 年）、近三年 vs 更早的份额变化与
  升温/降温判定、中位出版年、近期年份断层。「近三年」的界由 `RECENT_FROM = 当前年 − 2`
  算，**不再写死 2024**（写死过一年就失准）。
- **§6 值得注意的发现**：分类集中度、综述分布、**方法学缺项**（常用方法在哪些类里
  完全缺席的交叉表）、语料自查（重复 DOI / 重复标题 / 标签覆盖率 / 覆盖最薄的方向）。
  ⚠️ 它只给**结构性事实**——「结论互相矛盾」「方法学机会」这类要读全文的判断仍得人工写
  （`分类与标签总表.md` 的 §4 就是人工的，`sync_classification.py` 按锚点保护它）。
- **§7 被引与推荐分**：被库内引用最多的 10 篇、**孤立文献**（既没被库内引用、也没引用
  库内文献，实测 11 篇）、推荐分分档与各分类中位分。数据来自 `citation_graph.json` 与
  `library_scores.json`，**两者缺失时整段降级成一句说明**，不会让画像跑不出来。

⚠️ **「热点词」「新兴方向」两节 2026-09-20 已从画像里去掉**：`phrases()` 抽短语时不滤数字
与单位，出来的是 `mah g` / `3 v` / `4 3` / `0 5` 这类碎片，登在画像里纯属噪声。
`hot` / `emerging` 的计算暂时留在代码里（`hot` 还接着检索式扩写那条路），
但那条路**从未生效过**——`profile.json` 的 `queries` 与 `topic.json` 的基础式逐字相同，
`hot_phrases` / `emerging_phrases` 也没有任何消费者。

⚠️ **那套短语的排序必须是确定性的**（2026-09-20 修）：`freq_*` 这些 Counter 是拿
`set(phrases(b))` 喂出来的，而 set 的迭代顺序受哈希随机化影响——只按 `-count` 排序时，
并列项的顺序**每次运行都不同**，实测连跑两次 `hot_phrases` 就不一样。画像的 `queries`
由 hot 生成，漂移会顺带传到日报的检索式。现在排序带短语做次级键（`(-count, phrase)`）。

⚠️ **OpenAlex 2026 起按请求计费**（$0.001/条，匿名池每天 $0.1 ≈ 100 条），额度耗尽返回
429 + `retry-after`，重置在**次日 00:00 UTC**（= 08:00 CST）。`journal_if.py` 遇到 429 会在
第一个失败的刊处收工、把已查到的写盘并提示，不会逐条空转（旧写法每个刊要睡 200 秒）。
所以 `state/journal_if.json` 的 `missing` 里可能是「今天没查到」而不是「真的没有」——
额度重置后重跑 `journal_if.py` 即可补齐。已修的取错源问题：`Angewandte Chemie` 原先取到
OpenAlex 的**德文版**（2.2），而白名单靠前缀匹配把 `Angewandte Chemie International Edition`
（15.3）也归在这个名字下；现已写进 `journal_if.py` 的 `NAME_ALIAS` 并从缓存改正表值。

### 6.8 `summary.md` 的属性区字段

**属性区（Properties）不由模型写**。`summary.md` 的正文是 `kimi -p` 读 MinerU 转出的
`paper.md` 写的，而 MinerU 常解析不出 DOI、刊名、卷期页——模型只能写 `unknown` 或按
正文猜（实测 147 篇里 **39 篇**的 `doi` 是 `unknown`）。这些字段 Zotero 建条目时就有了
（CrossRef 或出版商页面），所以改由 `sync_properties.py` 从 Zotero 写入，模型只管正文。

| 字段 | 来源 |
|---|---|
| `title` | Zotero 标题（原文，不是模型转写的） |
| `author` | 全部作者，Zotero 顺序，逗号分隔 |
| `year` | `date` 里的 4 位年份 |
| `journal` | 期刊**全称**（`build_profile.py` 的刊名统计依赖它，别改成简写） |
| `cite` | 短引用：`<末位作者全名>, <年份>, <期刊简写>, <卷期页>, <DOI>` |
| `doi` | DOI |

- `cite` 的作者取**末位作者全名**（本领域的通讯作者惯例）：`Yang-Kook Sun`，不是 `Sun`。
  Zotero/CrossRef 都没有通讯作者标记，末位作者是最接近的近似。
- 期刊简写查 `state/journal_abbr.json`（全称 → ISO4 简写，**手工可改**）；
  表里没有的刊自动补（CrossRef `short-container-title` → Zotero `journalAbbreviation`
  → 全称），已有条目不会被覆盖，所以手工改过的值留得住。
- Zotero 某个字段为空时**沿用旧属性区的值**（模型可能从正文里正确抄到了），
  不会被覆盖成空；`cite` 的构件少于 3 个时留空，不写半截引用。
- `summarize_batch.py` 生成后自动调这套逻辑写属性区；老文件不重跑模型也能补，
  直接 `python3 scripts/sync_properties.py --apply`。

---

## 7. Zotero 本地 API

> 来源：2026-09-10 对 87 篇文献库做批量分类、标签写入、重复合并、空目录清理的完整过程。
> 环境：Zotero 10.0.2（官方 tar 包，数据目录 `~/Zotero`）+ Ubuntu 24.04。
> 本节保留原文档的小节编号（`### 0.` … `### 12.`），内部交叉引用写成 `§7.<小节号>`。
> 上手要用的授权步骤见 §3.2，可复用代码见 §7.11。

### 0. 一句话总结

Zotero 10 的本地 API **能读能写、免 API key、免云配额、离线可用**，是目前脚本化文献管理最好的入口。
但它有几个极度反直觉的行为，其中「写入字段必须平铺」这一条**静默失败**（返回 HTTP 200 却什么都没改），
是本轮踩得最深的一个坑。

### 1. 环境准备

#### 1.1 打开本地 API 开关

图形界面：**设置 → 高级 → 「允许其他应用与本机 Zotero 通信」**（默认关闭）

命令行方式（**Zotero 必须先关闭**，否则退出时会重写 `prefs.js` 覆盖你的修改）：

```bash
# 写 user.js 而不是改 prefs.js —— user.js 在启动时生效且不会被覆盖
cat > ~/.zotero/zotero/<profile>/user.js <<'EOF'
user_pref("extensions.zotero.httpServer.localAPI.enabled", true);
EOF
```

对应的 pref 名就是 `extensions.zotero.httpServer.localAPI.enabled`。

#### 1.2 端口与地址

```
http://localhost:23119/api/users/0        # 0 = 本地用户库
```

端口来自 `extensions.zotero.httpServer.port`（默认 23119）。
同一个端口上还跑着浏览器插件的 `/connector/*` 端点，可用来判断 Zotero 是否在运行：

```bash
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:23119/connector/ping   # 200 = 活着
curl -s -o /dev/null -w "%{http_code}\n" "http://localhost:23119/api/users/0/items?limit=1"
#   200 → 本地 API 已开启
#   403 → 开关没开（这就是最典型的症状）
```

**Zotero 必须在运行**，本地 API 没有服务端常驻方案。

### 2. 写入授权（每个应用一次）

读操作完全免认证。**写操作**需要一把本地密钥，通过下面这个端点申请：

```bash
curl -X POST "http://localhost:23119/api/local/authorize" \
  -H "Content-Type: application/json" \
  -H "Zotero-Server-ID: $SID" \
  -d '{"appName":"你的应用名"}'
```

调用后 Zotero 会**弹出对话框**让你选「允许 / 始终允许 / 拒绝」。选「始终允许」后：

```json
{"key":"04cB9QTKO9UNhf1umsHgcYZBEsrunZ9J","remember":true}
```

`remember: true` 表示密钥已保存，后续可复用，cron 无人值守也能写。
密钥可在 Zotero 设置里撤销。

### 3. 三个必需请求头（缺一个必报错）

| 请求头 | 用途 | 缺失时的报错 |
|---|---|---|
| `Zotero-Server-ID: <值>` | **所有写操作**，值在任意响应的响应头里 | `428 Zotero-Server-ID not provided` |
| `Authorization: Bearer <本地密钥>` | 所有写操作 | `401 API key required -- POST /api/local/authorize to obtain one` |
| `If-Unmodified-Since-Version: <n>` | **DELETE 操作必需** | `428 If-Unmodified-Since-Version not provided` |

Server-ID 的取值方式（每次响应都会带）：

```python
SID = urllib.request.urlopen(urllib.request.Request(
        "http://localhost:23119/api/users/0/collections?limit=1",
        headers={"Zotero-API-Version": "3"})).headers["Zotero-Server-ID"]
```

### 4. 【最大的坑】写入载荷的字段必须平铺

**读回来**的结构是嵌套的：

```json
{"key":"ABCD1234","version":123,
 "data":{"title":"...","tags":[{"tag":"x"}],"collections":["KEY1"]}}
```

**写进去**必须是平铺的：

```json
{"key":"ABCD1234","version":123,
 "title":"...","tags":[{"tag":"x"}],"collections":["KEY1"]}
```

如果照着读回来的结构把字段塞进 `data` 里提交，服务端会认为「这个对象没有任何变化」，
**返回 HTTP 200 + `unchanged`，什么都不改** —— 不报错、不警告、静默失败。

这一轮我第一遍写 87 篇全部返回 `unchanged`，排查后才定位到是这个原因。
**判断写入是否真的成功的唯一可靠方法，是看响应里的 `successful` 而不是 HTTP 状态码。**

### 5. 响应结构：取 key 要用 `success` 不是 `successful`

```json
{
  "successful": { "0": { ...完整对象..., "key": "XXXX", "version": 9 } },
  "success":    { "0": "XXXX" },
  "unchanged":  {},
  "failed":     {}
}
```

- `successful` 里是**对象**（取 key 要再剥一层）
- `success` 里是**key 字符串**

一开始用 `successful["0"]` 当 key 用，直接导致后续请求把 dict 当成字符串拼接而报错。

### 6. 列表端点会过滤：看不见的目录依然存在

`GET /collections` **不返回空目录**。本轮库里 33 个目录，API 只列出 7 个 —— 另外 26 个空目录
既不在列表里，也没法通过遍历发现。

但**按 key 单独访问是可以的**：

```bash
curl http://localhost:23119/api/users/0/collections/ABCD1234   # 200，能读到
```

这意味着：

- 想清理空目录，**必须先知道 key** —— 只能从数据库里拿
- 想完整备份 Zotero 库，**光靠 API 枚举是不够的**

顺带发现：一个**非空**目录也可能被隐藏。本库里 `Cell_paremeters` 明明装着 1 篇文献，
却不出现在 `/collections` 列表里（它是个孤立的遗留目录）。所以别把 API 列表当权威。

### 7. 删除语义：全是硬删除，没有回收站

| 操作 | 端点 | 后果 |
|---|---|---|
| 删条目 | `DELETE /items/<key>` | **硬删除**，不进回收站，`/items/trash` 里也找不到 |
| 删目录 | `DELETE /collections/<key>` | **硬删除**，API 无法撤销 |

⚠️ 2026-09-14 实测更正：本文件旧版写「删条目会进回收站、可在 Zotero 里恢复」，
**是错的**。本地 API 删掉 4 个条目后核验：

```bash
# 1) 条目已从 items 表消失，deletedItems 表 0 行
#    （Zotero 的回收站会把条目留在 items 表里再往 deletedItems 插一行）
# 2) 回收站端点确认空
curl -s -D- "http://localhost:23119/api/users/0/items/trash?limit=5"
# → Total-Results: 0 ；body: []
# 3) 只留下 syncDeleteLog 里的删除流水，那不是恢复路径
```

所以：**`DELETE /items/<key>` 与 `DELETE /collections/<key>` 一样不可撤销**。

**级联陷阱**：删除父目录会**连带删除其所有子目录**。之后再去删那些子目录会得到 `404`
—— 不是删除失败，而是它们已经跟着父目录一起没了。

本轮 26 个空目录里有 5 个报 404，全都是父目录先被删除后连带干掉的。

**删除前务必备份**：删条目前先把条目 JSON 导出到 `state/`
（见 `state/items_backup_2026-09-14.json` 的写法：条目全字段 + 目录名 + 子附件），
再连 `zotero.sqlite` + `storage/` 一起备份。删除是真的没有后悔药。

> 脚本侧：`python3 scripts/tidy.py` 默认**只报告**，`--apply` 前会把要删的东西全量导出到
> `state/tidy_backup_<日期>.json`（见 §5.3、§6.6.5）。

### 8. 标签与目录的写入格式

```jsonc
{
  "key": "...", "version": 123,
  "tags": [{"tag": "相变"}, {"tag": "方法:DFT"}],   // 必须是对象数组，不能是纯字符串数组
  "collections": ["KEY1", "KEY2"]                  // 覆盖式！必须带上原有的全部 key
}
```

`collections` 是**全量覆盖**语义，不是追加。想「加一个目录」必须先把原有列表读出来再合并，
否则会静默丢掉其它归属。

`tags` 同理，是全量替换 —— 但按 `tag` 字符串去重合并即可。

### 8.5 附件文件上传（三段式）

本地 API 走的是和 Web API 一样的三段式上传。给已有的空附件补文件时：

```python
# --- 1) 请求上传授权 ---
body = urlencode({"md5": md5, "filename": fn,
                  "filesize": len(data), "mtime": int(os.path.getmtime(p)*1000)})
POST /items/<attachmentKey>/file
Headers: Zotero-Server-ID / Authorization / Content-Type: application/x-www-form-urlencoded
         If-None-Match: *          ← 新文件必须带，否则报错
# → {"url":"http://localhost:23119/api/local/uploads/<key>",
#    "contentType":"application/pdf","prefix":"","suffix":"","uploadKey":"..."}
#   或 {"exists":1} 表示服务端已有同 md5 的文件

# --- 2) 上传字节 ---
POST <url>
Headers: Content-Type: <上面返回的 contentType>
Body: prefix.encode() + 文件字节 + suffix.encode()

# --- 3) 注册 ---
POST /items/<attachmentKey>/file
Body: upload=<uploadKey>            ← 同样要 If-None-Match: *
```

**几个要点：**

- `mtime` 单位是**毫秒**，不是秒
- `prefix` / `suffix` 本地模式下通常为空字符串，但一定要拼上（Web API 的 S3 模式会用到）
- 上传成功后文件落在 `~/Zotero/storage/<attachmentKey>/<filename>`

**验证上传是否成功，不要读 API 的 `path` 字段** —— 对 `imported_file` 类型它返回 `None`。
正确做法是看响应里的 enclosure 链接，或直接查文件系统：

```bash
ls -la ~/Zotero/storage/<attachmentKey>/
```

**一个必须注意的前置检查**：如果已有附件是**网页快照**（`linkMode=imported_url`、
`contentType=text/html`），不能往里塞 PDF —— 那样记录的元数据会和实际文件不符。
这时应该**新建一个附件条目**再上传：

```python
{"itemType":"attachment", "linkMode":"imported_file", "parentItem":"<文献key>",
 "title":"Full Text PDF", "filename":"xxx.pdf", "contentType":"application/pdf",
 "tags":[], "collections":[], "relations":{}}
```

本地 API 的**文件上传只接受 `imported_file` 和 `imported_url`** 两种模式。

### 9. 安全地读数据库

列表端点的过滤（§7.6）意味着有些信息只能从 SQLite 拿。安全做法：

```bash
# 三个文件必须一起复制，否则 WAL 里的最新改动读不到
cp ~/Zotero/zotero.sqlite     /tmp/zt.sqlite
cp ~/Zotero/zotero.sqlite-wal /tmp/zt.sqlite-wal
cp ~/Zotero/zotero.sqlite-shm /tmp/zt.sqlite-shm
python3 -c "import sqlite3; sqlite3.connect('/tmp/zt.sqlite')"   # 正常打开会自动应用 WAL
```

**两个必须记住的点：**

1. **不要用 `immutable=1`** —— 它会**忽略 WAL 文件**，让你读到迁移前的旧快照。
   我在核对 Zotero 15→16 的库迁移结果时被这个坑到，读到的数据是过期的。
2. **永远不要写数据库**。Zotero 官方原话：Zotero 运行时修改数据库
   「极易导致数据库损坏」，而且表结构随版本变化。读写都走 API。

### 10. 错误码速查

| 码 | 含义 | 处理 |
|---|---|---|
| `401` | 缺 `Authorization: Bearer` | 走 §7.2 申请密钥 |
| `403` | 本地 API 开关没开 | 设置 → 高级 → 打开开关 |
| `404` | 对象不存在，或已被父级联删除 | 重新枚举确认 |
| `405` | 二进制 diff（`PATCH .../file`）本地不支持 | 改用完整上传 |
| `428` | 缺 `Zotero-Server-ID` 或 `If-Unmodified-Since-Version` | 见 §7.3 |
| HTTP 200 + `unchanged` | **字段嵌套错了**（§7.4）或真的没变化 | 检查载荷是否平铺 |
| HTTP 200 + `failed` 里 `0 is not a valid named parameter` | 新建条目时某个字段的**类型**不对 | 见 §7.10.1 |

#### 10.1 建条目时 `0 is not a valid named parameter`

`POST /items` 新建 `journalArticle`，若 `ISSN` 传**数组**（CrossRef 返回的原始形状），
整个请求失败并回一句与字段无关的 SQL 报错：

```
HTTP 200 {"successful":{},"failed":{"0":{"code":400,
  "message":"Error(s) encountered during statement execution:
  0 is not a valid named parameter. [QUERY: SELEC..."}}}
```

`ISSN` 要传**字符串**（多个用 `", "` 拼）。其余字段（`date`/`volume`/`pages`/`url`/
`libraryCatalog`/`abstractNote`）传 CrossRef 的形状都没问题。

排错办法：先用最小载荷 `{itemType, title, DOI, creators, tags, collections, relations}`
建出条目，再逐个字段 `POST` 更新，失败的字段一眼可见。

### 11. 可复用代码

`~/LitHub/scripts/zapi.py` 封装了本轮所有需要的操作：

```python
import sys; sys.path.insert(0, 'scripts')
import zapi

cols  = zapi.get_all('collections')          # 自动分页
items = zapi.get_all('items/top')
d, h  = zapi.get('collections/XXXX')         # 单个对象（返回 data, headers）
st, r = zapi.write('POST', '/items', [obj])  # 自动带齐三个头
st, r = zapi.write('DELETE', '/collections/XXXX', version=9)
kids  = zapi.children('ITEMKEY')
```

其余脚本：

| 脚本 | 用途 |
|---|---|
| `mineru_batch.py` | PDF → Markdown 批量转换（含 gs 预压缩、分块、断点续跑） |
| `summarize_batch.py` | 逐篇生成中文总结（`kimi -p` 独立上下文） |
| `digest.py` | 从总结抽取紧凑清单，供分类使用 |
| `apply_to_zotero.py` | 把分类与标签写回 Zotero（纯新增） |
| `sync_classification.py` | 把 `classification.json` 渲染成「分类与标签总表.md」（只读分类数据，重跑安全） |

> 现存脚本的完整 CLI 参数见 §6.4–§6.6。

### 12. 关于是否要建 SKILL

**结论：值得建，但必须拆成两层，不要做成一个大而全的 skill。**

#### 12.1 拆法

| 层 | 形态 | 放什么 |
|---|---|---|
| 知识层 | Skill `zotero-local-api`（`~/.kimi-code/skills/`） | 本文档第 3–10 节：请求头、平铺载荷、列表过滤、删除语义、错误码 |
| 执行层 | `~/LitHub/scripts/*.py` | 抓取、转换、总结、写回等可重复的机械操作 |

**判断依据**：skill 是给模型看的**文档**，脚本是能真正**执行**的代码。
凡是「需要精确重复执行」的都该写成脚本；凡是「不查就会踩、但每次做法相同」的才该写成 skill。

#### 12.2 为什么值得建 skill

§7.4 那个平铺字段的坑，让我白写了一轮 87 篇（HTTP 200 却全部静默失败）。
这种知识的特征是：

- **反直觉**（照着响应结构写就对不上）
- **静默**（不报错，只看状态码发现不了）
- **稳定**（Zotero 10 的 API 不会天天变）
- **每次都会用到**（只要碰 Zotero 写入）

这正是 skill 的最佳适用场景 —— 一次性投入，之后每次省掉一轮排查。

#### 12.3 为什么不建议把整条流水线做成 skill

- 流程会随研究方向和库结构变化，写死了反而僵化
- 抓取/转换/总结这类操作已经由脚本承担，skill 里重复描述没有增益
- 但 **API 的坑是不变的知识**，值得固化

#### 12.4 建议的落地方式

1. 建用户级 skill `zotero-local-api`，内容就是本文档第 3–10 节
2. 在 `~/LitHub/AGENTS.md` 写项目级约定（工作流、目录结构、脚本入口），
   这样每次在 `~/LitHub` 里开 kimi 会话都会自动加载
3. 脚本保持独立，不进 skill

### 附：找付费墙论文的 PDF（可复用经验，不绑定任何具体文献）

**有效的顺序**（按命中率从高到低）：

1. **OpenAlex** `best_oa_location.pdf_url`，再遍历 `locations[].pdf_url` —— 命中最多
2. **Unpaywall** `url_for_pdf` —— 与 OpenAlex 同源，增量有限
3. **Semantic Scholar** `openAccessPdf.url` —— 会再补几篇
4. 站点特有的直链模式（**API 只给 landing page，PDF 直链要手工拼**）：
   - OSTI：`https://www.osti.gov/servlets/purl/<id>`
   - eScholarship：landing 是 `/uc/item/<hash>`，PDF 直链是
     `/content/qt<hash>/qt<hash>.pdf`
   - EuropePMC：`https://europepmc.org/backend/ptpmcrender.fcgi?accid=PMC<id>&blobtype=pdf`

**失败原因分类**：`nature.com` 与 `europepmc` 对脚本下载返回 HTTPError；
Wiley / Elsevier 的正刊基本都付费墙。这几类只能靠校园网或图书馆。

**两条必须遵守的规矩**：

- 下载脚本要**逐个候选链尝试**，不要赌第一条
- **必须校验下到的确实是 PDF**（前 5 字节为 `%PDF-`，且 `pdfinfo` 能解析出页数）——
  否则会把 HTML 落地页当成 PDF 存下来

---

## 8. MinerU 与文档转换

> 截至 2026-09-14，基于把 104 篇文献从 PDF 转成 Markdown、并给总结配图的全过程。
> Zotero 侧的坑见 **§7**；操作入口见 **§6.6.1**。
> 本节保留原文档的小节编号（`### 0.` … `### 7.`），内部交叉引用写成 `§8.<小节号>`。

### 0. 一句话总结

**`flash-extract` 是纯文本预览档**（图表变占位符），**`extract` 是精准档**（导出图片，
需 token）。要图就必须用 `extract`；token 复制的常见失误是**不完整**（正确长度 51 字符），
而报错文案会把你误导成"过期"。

### 1. Token：报错文案不可信

#### 1.1 症状与真相

```
HTTP 401 {"traceId":"...","msgCode":"A0211","msg":"user token expired"}
```

这句话**没有诊断力**。实测对照：

| 送进去的令牌 | 服务器回答 |
|---|---|
| 用户给的真令牌 | `A0211 user token expired` |
| 我编的 `sk-0000…0000` | `A0211 user token expired` ← **一模一样** |
| 不带 Authorization | `login required` |
| 去掉 `sk-` 前缀 | `A0202 user authenticate failed` |

所以 A0211 的真实含义是「这串我不认」，把「格式对但不存在」「真过期」混在同一句里。

#### 1.2 两个"验证"都是假的

- `mineru-open-api auth --verify` 只查**格式**，它自己都提示
  "full verification requires an API call"。喂 `sk-test`（7 字符）也说 "Token format is valid"。
- 控制台把 key 显示成 `sk-xxxxx*****xxxxx`，**看不出真实长度**。前两把只复制到 35 位，
  真正能用的那把是 **51 位**——这才是"expired"的真正原因。

#### 1.3 正确的验证方法

```bash
curl -s -X POST https://mineru.net/api/v4/file-urls/batch \
  -H "Authorization: Bearer <完整key>" -H "Content-Type: application/json" \
  -d '{"files":[{"name":"t.pdf"}],"model_version":"vlm"}'
# 有效 → {"code":0,"msg":"ok","data":{"batch_id":"...","file_urls":["https://mineru.oss-..."]}}
# 无效 → A0211 / A0202
```

排查时按这个顺序走，别在 CLI 和配置文件上绕：① 令牌长度对不对（51）→ ② 上面这条 curl
→ ③ 本机时钟与 `mineru.net` 响应头的 `Date` 是否一致 → ④ `update --check` 是否最新。

**已被排除、不必再查的**：本地配置文件写错（逐字节核对过）、网络篡改认证头（用 httpbin
回显验证过原样送达）、主机名写错（只有 `mineru.net` 能解析）、接口版本过时（v4/v5 都在
认证层被拒）。

### 2. 两个引擎：差别不只是质量

| | `flash-extract` | `extract`（精准，默认） |
|---|---|---|
| token | 不需要 | **需要** |
| 图表 | `<!-- image-->` **占位符，无路径** | `images/<sha256>.jpg` + `![]()` 引用 |
| 公式/表格 | 占位符 | 识别 |
| 限额 | 10 MB / 20 页（**大小先触发**，`--pages` 绕不过） | 200 MB / 600 页 |
| 对策 | >8 MB 先 `gs -dPDFSETTINGS=/ebook`，>20 页分块 | 无需压缩、无需分块 |
| 实测速度 | 30–160 s/篇 | 28 s–324 s/篇 |

`-o` 到目录时，`flash-extract` **只产出一个 `.md`**；`extract` 产出 md + `images/`。
把「md 所在的整个目录」搬到论文目录即可，**不要假设目录命名**，相对路径自然可用。

#### 限流（官方）

提交任务 50 文件/分钟（单文件/批量/URL 三种共用）、每日 5000 文件（html ≤100）；
取结果 1000 次/分钟。目前**无收费计划**。

### 3. 图片：一图常被拆成多张

**每张图的图片文件是若干张连续排列的，图注在最后一张之后**，中间可能夹着 `a` `b` `c`
这类单字符行。实测 `Figure 3` 由 3 个文件拼成：

```
![](images/cbf2c716….jpg)
![](images/873a9713….jpg)
![](images/da0bced2….jpg)
Figure 3. Li migration pathways across and along the Σ2 GB. ...
```

所以引用某张图必须**往回收集到上一段正文为止**的所有图片，只取紧邻的那一张会漏掉大部分面板。

**图注格式因出版商而异**，写解析要一并覆盖：

| 出版商 | 形式 |
|---|---|
| Elsevier / ACS | `Figure 1. ...` / `Fig. 1. ...` |
| Nature 系 | `Fig. 1 \| ...`（竖线，**不是句点**） |
| 部分期刊 | `Fig. 4a and b show ...`（这是**正文引用**，不是图注，不能当图注解析） |

#### 陷阱：正文句子会被当成图注

最坑的一个 bug：正文里

```
Fig. 8 shows the temperature dependence of the thermodynamic energies of materials A and B.
...
![](images/b600b13d….jpg)
Fig. 8. The thermodynamic energies of materials A and B.
```

**两行都以 `Fig. 8` 开头**。若按"首个匹配"取图注，拿到的是正文那句，它前面没有图片
→ 这张图被判为「找不到图片」，静默丢失。实测 **21 篇**受影响、丢掉 **24 张图**。

**正确做法**：对每个图号收集**所有**候选行，取「前面挂了图片最多」的那一条当图注。
真正的图注前面必有图片，正文句子前面通常是正文。同理，面板收集要往回走到上一段正文为止，
中间允许夹 `a` `b` `c` 这类单字符行。

#### 陷阱：有图但没图注

少数 PDF（实测 **4/103**）MinerU 会导出 `images/` 却不输出图注，正文里只有
`Fig. 2 schematically shows …` 这类引用句。这种**无法把图片与图号、与图注对应起来**，
只能放弃配图——不要靠猜测给图随便编号。

### 4. 控制字节（NUL）：成因与修法

#### 4.1 成因

PDF 的**字体 ToUnicode 映射损坏**时，抽取器解不出字形就写 U+0000。受害的不只是连字
（`different`、`effect`、`diffraction`），还有度符号、上标负号、乘号、希腊字母、空间群横杠：

```
di●erent → different        mAh g<sup>●</sup><sup>1</sup> → g⁻¹
940●C → 940°C               1.55 ● $10^{-12}$ → ×
180● Ni-O-Ni → 180°          R●3m → R-3m
fracture toughness ● → Γ     where ● is the partial molar volume → Ω
```

#### 4.2 后果比想象中严重

含 NUL 的文件被当成**二进制**：`grep` 只报 "binary file matches" 而不显示内容，
严格的阅读器/工具直接拒绝打开（本仓库的 `Read` 工具就明确拒读）。

#### 4.3 修法

换 `extract` 引擎能修掉大部分（实测 **289 → 118 处**，连字全清）；剩下的按上下文规则补，
脚本见 `scripts/fix_control_bytes.py`（`--apply` 才写入，原件备份到 `state/trash/`）。

**两条硬规矩**：

1. **判定不出的一律不猜**，用 `【?】` 标记并写进 `state/control_bytes_fix.json`。
   实测有极少数位置**连 `pdftotext` 也解不出**（给出 U+0001/U+0003 之类的控制符），
   说明字形在 PDF 里就丢了——这种位置任何"修复"都是编造。
2. **占位符不能用 `□`（U+25A1）**。本库里 `□` 是**晶格空位**的合法记号
   （`Li4/7[□1/7Mn6/7]O2`），第一版用它当占位符，结果 12 个 `□` 里 6 个是原有的、
   6 个是我加的，完全分不清。改用 `【?】` 后界限清楚。

### 5. 视觉能力：本环境没有

这一条影响很大，别绕弯路。**三条路都试过**：

1. 主 agent 的 `Read` 工具：对图片直接报 `is an image file. Only text files can be read.`
2. `explore` / `plan` 子 agent：工具清单里虽列了 `ReadMediaFile`，**实际拿不到**，
   只拿到元数据（尺寸、格式、`file` 输出）
3. 本机 OCR：`tesseract` 未安装（`PIL`、ImageMagick、`poppler` 有）

#### 5.1 由此产生的两个坑

- **`summarize_batch.py --figures` 无效且有害**。让 `kimi -p` 去读图，实测行为不一致：
  一篇诚实追加「本次总结未能读取图片」并白花 **407 秒**（纯文本只要 60–130 秒，它在反复
  尝试）；另一篇**不加声明**，反而写了 5 处图描述，读起来像看过图——核对下来是照图注
  转述的（内容无误、文件名也都真实存在），但把"转述图注"呈现成"观察结果"是误导的。
- **任何图描述都必须交代来源**。要图就**把图片嵌进 Markdown**（`![](images/xxx.jpg)`），
  让读者自己看；文字说明一律据图注与正文，并注明未判读图像内容。

### 6. 省 token 与防幻觉（配图方案）

给 100+ 篇总结配图时，两条设计决定了成本与可靠性：

#### 6.1 只喂图注，不喂全文

模型不需要读 100 KB 全文来判断哪张图重要。只给：标题 + 摘要 + 结论 +
**每张图的英文图注 + 正文中引用该图的句子**，上下文从约 100 KB 降到 **3–7 KB（约 1/10）**。
实测每篇 20–28 秒（对比读全文的 407 秒）。

#### 6.2 模型只做判断，不写路径

**绝不让模型输出图片文件名**——它可能编出看似合理的 sha256 文件名。
正确分工：模型只输出「图号 / 中文标题 / 图注中文 / 为什么关键」，
**图片路径由脚本从 `paper.md` 解析后填进去**。

再叠一道校验：模型给的图号必须在 `paper.md` 里真实存在且有图注，否则丢弃并记录。
实测有效——某篇模型编了个不存在的「图 5」，被脚本拦下并记入日志。

### 7. 流水线要点

- `paper.md` 的 front-matter 记 `engine:`，**跳过逻辑必须按引擎判断**：flash 转的遇到
  extract 要重转，否则永远拿不到图。
- 全库切 extract 后 `papers/` 从约 7 MB 涨到 113 MB（3252 张图）。
- 仍有小概率拿不到图：① Zotero 里没有 PDF 附件（靠全文缓存转的，必然无图）；
  ② 个别 PDF 解析器不吐图（实测 1/90，正文完好但零图片引用）；
  ③ 个别 PDF 直接解析失败（`retry limit reached … please replace the file`，需换文件）。

---

## 9. 定时任务

以 systemd 系统级 timer 为例。**把 `<你的用户名>` 换成你的用户名，路径换成你的实际路径**：

```ini
# /etc/systemd/system/lithub-daily.service
[Unit]
Description=LitHub 文献日报
After=network-online.target
Wants=network-online.target

[Service]
Type=oneshot
User=<你的用户名>
Group=<你的用户名>
Environment=HOME=/home/<你的用户名>
Environment=PYTHONUNBUFFERED=1
Environment=LITHUB_MAILTO=<你的邮箱>
WorkingDirectory=/home/<你的用户名>/LitHub
# 裸跑：工作点全部交给 config/runtime.json，改配置不必动 unit（见 §4.3）
# ⚠️ 这里若写死 --limit / --days，会盖过 runtime.json（CLI 优先）
ExecStart=/usr/bin/python3 /home/<你的用户名>/LitHub/scripts/daily_digest.py
# 可选：出完报立刻发信（带 - 前缀，发信失败不让 unit 变 failed）
ExecStartPost=-/usr/bin/python3 /home/<你的用户名>/LitHub/scripts/send_digest.py
# 可选：顺手刷新库内推荐分（同上，带 - 前缀）
ExecStartPost=-/usr/bin/python3 /home/<你的用户名>/LitHub/scripts/score_library.py --apply
TimeoutStartSec=3600
Nice=10
```

```ini
# /etc/systemd/system/lithub-daily.timer
[Unit]
Description=每天 05:00 生成 LitHub 文献日报

[Timer]
OnCalendar=*-*-* 05:00:00
Persistent=true
AccuracySec=30s
Unit=lithub-daily.service

[Install]
WantedBy=timers.target
```

再配一个每月刷新画像的（`lithub-profile.service/.timer`，`OnCalendar=*-*-01 04:40:00`，
跑 `build_profile.py`），以及发信兜底的 `lithub-mailcheck.timer`（每天 06:00 跑
`send_digest.py`）。然后：

```bash
sudo systemctl daemon-reload
sudo systemctl enable --now lithub-daily.timer lithub-profile.timer lithub-mailcheck.timer

systemctl list-timers lithub-daily.timer lithub-profile.timer   # 看下次触发时间
systemctl status lithub-daily.service                           # 看最近一次结果
journalctl -u lithub-daily.service -n 50                        # 看 systemd 日志
sudo systemctl disable --now lithub-daily.timer                 # 停掉

# 手动触发一次
sudo systemctl start lithub-daily.service
```

- `Persistent=true`：关机错过后开机补跑。
- **profile 的 timer 要比 daily 早**，否则日报用的是上个月的画像。
- ⚠️ systemd **不读 `.bashrc`**，所以 `LITHUB_MAILTO` 必须写进 unit 的 `Environment=`
  （漏写的后果见 §3.9：不报错，只是慢且 `A` 恒为 0）。

### 9.1 三个 timer 的分工

- `lithub-daily.timer`：每天 05:00 **无条件**触发 `daily_digest.py`，产出
  `文献日报/YYYY-MM-DD.md` 与 `.urls.txt`；`Persistent=true`，关机错过后开机补跑。
- `lithub-profile.timer`：每月 1 日 04:40 跑 `build_profile.py`，先于当日日报刷新画像。
- `lithub-mailcheck.timer`：每天 06:00 跑 `send_digest.py` 兜底——当日日报已生成、
  且 `state/email_sent.json` 里当日计数 < 1 时补发一次；已发过或当天没日报就直接跳过。
  它兜的是「05:00 出报成功、但发出去那一步失败」（SMTP 抖动）这种情况。

### 9.2 频率闸门（2026-09-20 起）

timer 照旧每天触发，**跑不跑由脚本读 `config/runtime.json` 的 `schedule` 段自己决定**——
`enabled: false` 直接跳过；`every_n_days: N`（N>1）时距上次成功出报不足 N 天也跳过。
选中这条路而不是动态 enable/disable timer，是为了不让「timer 说跑、配置说不跑」各说各话。

- 跳过时**只在日志留一行写明原因**，不写日报、不动 `recommendations.json`；
- `--dry-run` / 显式 `--date` / `--force` 三者**一律放行**（预演要看真实结果、
  回填历史与手动强制都得跑得动）；
- 上次成功出报的日期记在 `state/last_push.json`，闸门**只看它**——不猜「日报文件在不在」，
  因为没候选、翻译全失败、手工删过报都会让文件与「跑过了」对不上。
  没候选那次不写它（=「没推送就不算跑过」，`every_n_days>1` 时第二天会再试一次）。

### 9.3 推送记录与冷却

推送状态在 `state/recommendations.json`（`first_pushed` / `last_pushed` /
`times_pushed` / `channel` / `score` / `status`）：`--cooldown-days`（默认 60）内同一
DOI 不再推，超期后高分老文可再推一次；旧的 `state/digest_seen.json` 首次运行会
迁移进来，旧文件保留不删。

---

## 10. 故障速查

| 现象 | 原因 / 处理 |
|---|---|
| 任何脚本 `FileNotFoundError: state/zotero_local_key` | 还没授权，或 Zotero 没运行。见 §3.2 |
| Zotero API 返回 403 | 「允许其他应用与本机 Zotero 通信」没勾（§7.10） |
| 写入返回 HTTP 200 但条目没变 | Zotero 本地 API 的静默失败，载荷字段必须**平铺**。见 §7.4 |
| 取 key 报「dict 不能当字符串」 | 用了 `successful` 而不是 `success`。见 §7.5 |
| 删目录报 404 | 父目录已被删，子目录被级联删掉了。见 §7.7 |
| 脚本卡很久才超时 | IPv6 陷阱，见 §3.8（实测 181 秒 vs 0.72 秒） |
| `A` 恒为 0、每篇多等 ~12 秒 | `LITHUB_MAILTO` 是占位邮箱，Unpaywall 422 硬拒。见 §3.9 |
| MinerU 报 401 `A0211 user token expired` | **该错误码没有诊断力**（编造的 key 也报它）。真正常见原因是**复制的 key 不完整**，正确长度 **51 字符**。见 §8.1 |
| MinerU `extract` 没图 / 公式是占位符 | 可能走了 `--flash` 兜底。`paper.md` 的 front-matter 有 `engine:`，flash 转的遇到 extract 会被重转（§8.7） |
| MinerU `retry limit reached … please replace the file` | 该 PDF 直接解析失败，需换文件（§8.7） |
| 论文里出现 `●` / `【?】` / 文件被当二进制 | 字体 ToUnicode 损坏产生 NUL。跑 `fix_control_bytes.py --apply`，`【?】` 是**判定不出**的标记。见 §8.4 |
| 总结全是 `EXC(...)` | LLM CLI 路径不对，`echo $LITHUB_KIMI` / `which kimi`。见 §3.4 |
| 总结报「未落笔」 | 步数上限太紧，模型把步数花在读上还没写。调大 `--max-steps`（§6.5.3） |
| 总结里说「未能读取图片」 | 正常——本环境模型读不到 `images/` 里的图。**别开 `--figures`**，要配图用 `embed_figures.py`（§8.5、§6.6.2） |
| 配图丢了很多张 | 图注解析被正文引用句骗了（`Fig. 8 shows …`）。脚本已按「前面挂了图片最多的候选行」判定；实测该 bug 会让 21 篇丢 24 张图（§8.3） |
| `daily_digest.py` 报 `--cat-floor ... 不是合法的一项` | `runtime.json` 的 `cat_floor` 里还是作者的分类名。改成你的分类，或留空 `""`（见 §4.3） |
| `runtime.py` 报「有认不出的键 / 必须是整数 / 必须 ≥ N」 | `runtime.json` 写错了。这是**刻意**的：宁可不跑，也不静默忽略一个你以为生效了的配置（§4.3） |
| 日报没出，日志里写「按 schedule 跳过本期」 | `runtime.json` 的 `schedule.enabled=false`，或 `every_n_days` 没到。要立即跑加 `--force`（§9.2） |
| 改了 `runtime.json` 的 `limit`/`days` 却没生效 | systemd unit 的 `ExecStart` 里写死了同名参数——**CLI 优先**。把参数从 unit 删掉（§4.3） |
| 改过 `pool.refresh_days` 后第一次跑特别慢 | 缓存键（周期桶）换了，query/refs 整体冷一次（实测约 28 分钟）；之后同周期内重跑零网络（§6.2.3） |
| 日报候选极少 / 全是离题 | `journals` 白名单、`gates` 正则没按 §4.1/§4.2 改 |
| 日报门槛挡下几乎全部候选 | `--min-score` 没重标定（尤其刚启用 `S`，见 §6.3.4） |
| 某个方向总也进不了版面 | **加权重换不来文献，必须同时加检索式**（§6.3.6） |
| 日报某条没有摘要 | Elsevier / Nature 系付费刊取不到（实测约占 30%），收编后从 `paper.md` 拿（§6.4） |
| `daily_digest.py` 直接崩 | `state/classification.json` 缺失（唯一无 try 保护的输入）。见 §5.4 的降级表 |
| OpenAlex 429 | 2026 起按请求计费（匿名池每天 $0.1 ≈ 100 条），重置在次日 00:00 UTC（08:00 CST）。额度恢复后重跑即可（§6.7） |
| HuggingFace 下不动 | `export HF_ENDPOINT=https://hf-mirror.com`（§3.10） |
| `tidy.py` 没删掉有子项的重复条目 | 这是默认行为，加 `--merge-dups` 才合并（§6.6.5） |
| 空目录删不掉某个 | 只有**叶子**目录会被删；父目录自己没条目但子目录里有文献的情况不会被删（§5.3） |
| Zotero 里条目列表看不到推荐分 | `Extra` 是原生列，要在条目列表表头右键 → 列 → 勾上 Extra（§6.6.6） |
| Obsidian 打不开链接 | `~/.local/bin/obsidian-open` 必须用 `xdg-open` 发 `obsidian://` URI —— 直接执行 `/snap/bin/obsidian "obsidian://..."` 时 snap 包装器不转发 URI，Obsidian 会静默忽略 |

---

## 11. 这个仓库有什么、没有什么

**有**（可直接复用的全部代码与文档）：

- `scripts/` 全部脚本（幂等、可续跑）
- `config/topic.json` —— 作者的领域配置（含五个标签轴词表），**当模板读**，不要直接用
- `config/runtime.json` —— 运行参数，**先改 `cat_floor`**（见 §4.3）
- `prompts/summarize.md` —— 总结模板
- `examples/NatureComm_2024_grain-level-chemo-mechanics/` —— 一篇 **CC BY 4.0** 开放获取论文
  经流水线处理后的完整产出（`paper.md` + `summary.md` + 图），**看输出长什么样就照它**
- `帮助手册.md`（本文，含上手路径、环境、换方向清单、全部参数与踩坑）、
  `AGENTS.md`（AI 代理的项目约定）、`README.md`

**没有**（涉及版权与个人数据，已剔除）：

- `papers/`（论文全文 Markdown、图片、PDF）
- `state/`（manifest 含本机绝对路径、classification、画像、引文图、推荐记录、**API 密钥**、各类缓存）
- `文献日报/`、`logs/`、`待整理/`
- 作者个人的工作稿（`分类与标签总表.md`、`文献画像.md`、`综述_*.md`、`新文献候选*.md`、`报告.md`）
- `.obsidian/`、`.git/`

所以你是**真正的从零开始**：所有 `state/` 文件由 §5 的流程生成，`papers/` 由你自己的 PDF 生成。

---

## 附：想继续深入时读哪里

| 想看什么 | 去哪 |
|---|---|
| 上手照做 | §1 |
| 换成自己的方向（含硬编码清单） | §4 |
| 某个参数什么意思、怎么调 | §6 |
| Zotero API 的坑与可复用代码 | §7 |
| MinerU 转换、图片、控制字节 | §8 |
| 出错了 | §10 |
| 这个脚本为什么这么写、项目内部决策与实测记录 | `AGENTS.md` |
